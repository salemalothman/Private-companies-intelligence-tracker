/**
 * Minimum password length. Enforced in the `signup` / `updatePassword` server
 * actions AND surfaced as a form hint — kept in one place so the validation and
 * the hint can never drift.
 */
export const MIN_PASSWORD_LENGTH = 6;
