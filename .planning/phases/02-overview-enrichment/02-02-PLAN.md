---
phase: 02-overview-enrichment
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - components/company/rating-indicator.tsx
  - components/company/overview-sections.tsx
  - app/(app)/companies/[id]/page.tsx
autonomous: false
requirements: [OVR-01, OVR-02, OVR-03, OVR-04, OVR-05]
user_setup: []
must_haves:
  truths:
    - "On the Overview tab with a stored analysis, Executive Summary renders pinned at the top and IC Conclusion (rating + bull/bear + recommendation) pinned at the bottom."
    - "Core Technology shows a 1-10 moat rating indicator; Strategic Moat shows four per-dimension 1-10 indicators."
    - "Unit Economics, TAM/SAM/SOM (directional ranges + confidence), Product Portfolio, Vertical & Customer Segments, Business Model, and Historical Analogue all render as collapsible sections."
    - "Every forward-looking field shows its ConfidenceChip; Outlook & Exit shows no probabilities or price targets."
    - "Before first generation the Overview tab shows the DeepDiveEmpty CTA, not empty section shells; existing Overview stats/description/BusinessModelAnalysis are preserved."
  artifacts:
    - path: "components/company/rating-indicator.tsx"
      provides: "Flat 1-10 rating bar/number primitive (design-system consistent)"
      contains: "RatingIndicator"
    - path: "components/company/overview-sections.tsx"
      provides: "Maps OverviewSections -> pinned cards + CollapsibleSections with ConfidenceChip + ratings"
      contains: "OverviewSections"
    - path: "app/(app)/companies/[id]/page.tsx"
      provides: "Overview tab wired to render the analysis sections"
      contains: "OverviewAnalysis"
  key_links:
    - from: "components/company/overview-sections.tsx"
      to: "components/dashboard/collapsible-section.tsx"
      via: "reuses CollapsibleSection / SectionEmpty"
      pattern: "CollapsibleSection|SectionEmpty"
    - from: "components/company/overview-sections.tsx"
      to: "components/company/confidence-chip.tsx"
      via: "renders ConfidenceChip per labelled field"
      pattern: "ConfidenceChip"
    - from: "app/(app)/companies/[id]/page.tsx"
      to: "components/company/overview-sections.tsx"
      via: "Overview TabsContent renders the analysis component"
      pattern: "OverviewAnalysis|overview-sections"
---

<objective>
Build the section-rendering component set that maps the typed `OverviewSections` (from Plan 01) into the Overview tab: Executive Summary pinned at top, IC Conclusion pinned at bottom, and every analytical section in between as a `CollapsibleSection` carrying `ConfidenceChip`s and 1-10 rating indicators. Wire it into the Overview `TabsContent`, preserving all existing Overview content, and show `DeepDiveEmpty` before the first run.

Purpose: This completes the vertical slice — Plan 01 produces the data, this plan lets a user actually SEE the full investment thesis on the Overview tab (OVR-01..05).

Output: A rating primitive, an overview-sections renderer, and the wired Overview tab; verified end-to-end against a regenerated demo company.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-overview-enrichment/02-CONTEXT.md
@.planning/phases/02-overview-enrichment/02-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor renders against. Plan 01 SUMMARY confirms final field names. -->

From lib/agents/deep-dive-types.ts (Plan 01 output — the shape to render):
- `OverviewSections` — typed per-section shape (executive_summary, technology{+moat_rating}, product_portfolio, vertical_customer, business_model, unit_economics, market_opportunity{tam,sam,som}, strategic_moat{switching_costs,network_flywheel,distribution_regulatory,ip}, historical_analogue, outlook_and_exit, ic_conclusion{rating,bull,bear,recommendation}). All fields optional.
- `LabelledField { text, basis, confidence, source? }` — narrative fields.
- `Rating1to10` — numeric ratings (1-10 or null).

From components/company/confidence-chip.tsx (Phase 1 — reuse, do not modify):
- `ConfidenceChip({ basis, confidence, className? })` — presentational, server-safe (no hooks, no icon props). Confidence literal is "med".
- `DeepDiveEmpty({ message?, action?, className? })` — the empty-state CTA (already wired in the page header; reuse for the Overview tab body).

From components/dashboard/collapsible-section.tsx (reuse, do not modify):
- `CollapsibleSection({ title, icon?, count?, defaultOpen?, className?, children })` — CLIENT component. An `icon` prop is a lucide `forwardRef` and cannot be passed from a Server Component; either omit `icon` or make the consumer a client component. Prefer OMITTING icons so overview-sections can stay a Server Component.
- `SectionEmpty({ children })` — muted empty row for absent sections.

From lib/types.ts:
- `CompanyAnalysisRow { sections: AnalysisSections; valuation: AnalysisValuation; generated_at; model; ... }` — page already loads this as `analysis` (see page.tsx line 104).

From app/(app)/companies/[id]/page.tsx (integration point):
- `analysis` (CompanyAnalysisRow | null) already fetched at line 104; `analysisStale` computed at line 118. The Overview `TabsContent value="overview"` is lines 337-374 with a Card > CardContent holding Stats, Founders, Description, and `<BusinessModelAnalysis company={company} />` (KEEP ALL of these).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RatingIndicator primitive (flat 1-10 bar + number)</name>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (§Design System — hairline border, tabular-nums, label-eyebrow, ink ramp; bg-foreground opacity ramp used in business-model-analysis.tsx)
    - components/company/business-model-analysis.tsx (the flat monochrome bar aesthetic to match)
    - components/company/confidence-chip.tsx (the quiet server-safe presentational style to match)
  </read_first>
  <action>Create components/company/rating-indicator.tsx exporting a presentational, server-safe (no hooks, no lucide-icon props) `RatingIndicator({ label, value, className })` where `value: number | null` is a 1-10 rating. Render a small label (label-eyebrow), the numeric value with `/10` in `tabular-nums`, and a flat 10-segment or single-fill hairline bar using the `bg-foreground` opacity ramp consistent with BusinessModelAnalysis (filled segments `bg-foreground`, empty `bg-muted`; 1px hairline via `border-border`). When `value` is null render an em-dash and no bar (never fabricate a rating). Keep it monochrome — no saturated brand color. Do NOT add "use client"; it must be usable from the Server Component renderer in Task 2.</action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "RatingIndicator" components/company/rating-indicator.tsx && ! grep -q "use client" components/company/rating-indicator.tsx</automated>
  </verify>
  <done>RatingIndicator exported, server-safe (no "use client", no hooks/icon props), renders value/10 with a flat hairline bar and em-dash for null; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: OverviewAnalysis section renderer (pinned cards + collapsibles + chips + ratings)</name>
  <read_first>
    - lib/agents/deep-dive-types.ts (OverviewSections / LabelledField / Rating1to10 — the exact field names from Plan 01)
    - components/dashboard/collapsible-section.tsx (CollapsibleSection/SectionEmpty API + the client-boundary icon rule — omit icons)
    - components/company/confidence-chip.tsx (ConfidenceChip + DeepDiveEmpty)
    - components/company/rating-indicator.tsx (RatingIndicator from Task 1)
    - .planning/phases/02-overview-enrichment/02-01-SUMMARY.md (confirm final section field names)
  </read_first>
  <action>Create components/company/overview-sections.tsx exporting a Server Component `OverviewAnalysis({ sections })` where `sections: AnalysisSections`. Render, in this fixed order: (1) Executive Summary as a pinned, non-collapsible card at the TOP (heading + thesis, value_prop, positioning, most_likely_outcome, strengths/weaknesses lists), each labelled field followed by its `ConfidenceChip`. (2) A stack of `CollapsibleSection` panels (omit the `icon` prop to stay server-safe), one per: Core Technology & Differentiator (narrative + `RatingIndicator label="Moat" value={technology.moat_rating}`), Product Portfolio, Vertical & Customer Segments, Business Model, Unit Economics, Market Opportunity (render TAM/SAM/SOM as three labelled directional ranges each with a ConfidenceChip), Strategic Moat (four `RatingIndicator`s: Switching Costs, Network/Flywheel, Distribution/Regulatory, IP + optional narrative), Historical Analogue, Outlook & Exit (narrative labelled fields only — render nothing numeric; do not invent probability/price rows). (3) IC Conclusion as a pinned card at the BOTTOM: a rating badge (map rating enum strong_buy/buy/hold/sell to a readable label via a small pure helper), bull field, bear field, recommendation — each with its ConfidenceChip. For any absent section/field, render `SectionEmpty` inside that panel (or omit the field) — never crash on undefined. Add a small internal `Field` helper that renders a LabelledField's text + ConfidenceChip together. Do NOT add "use client" — keep it a Server Component (that is why icons are omitted and ConfidenceChip/RatingIndicator are server-safe). Export any pure enum→label mapping so it could be unit-tested if needed.</action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "OverviewAnalysis" components/company/overview-sections.tsx && grep -q "CollapsibleSection" components/company/overview-sections.tsx && grep -q "ConfidenceChip" components/company/overview-sections.tsx && ! grep -q "probability\|price_target\|price target" components/company/overview-sections.tsx</automated>
  </verify>
  <done>OverviewAnalysis renders Executive Summary pinned top, all middle sections as icon-less CollapsibleSections with ConfidenceChips + RatingIndicators, IC Conclusion pinned bottom with a rating badge; absent fields degrade to SectionEmpty; no probability/price-target rendering; server-safe; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire OverviewAnalysis into the Overview tab</name>
  <read_first>
    - app/(app)/companies/[id]/page.tsx lines 90-120 (analysis + analysisStale already computed) and lines 336-374 (Overview TabsContent — the exact block to extend)
    - components/company/overview-sections.tsx (OverviewAnalysis API from Task 2)
    - components/company/confidence-chip.tsx (DeepDiveEmpty) and components/company/deep-dive-button.tsx (DeepDiveButton — the action slot already used in the header)
  </read_first>
  <action>Edit app/(app)/companies/[id]/page.tsx. Import `OverviewAnalysis` from "@/components/company/overview-sections". Inside `TabsContent value="overview"`, AFTER the existing Card (Stats + Founders + Description + BusinessModelAnalysis — leave that Card exactly as-is per behavior-preserving), add the analysis rendering: when `analysis` is present render `<OverviewAnalysis sections={analysis.sections} />`; when `analysis` is null render `<DeepDiveEmpty action={<DeepDiveButton companyId={company.id} />} />` (DeepDiveEmpty is already imported; import DeepDiveButton if not already in scope — it is imported at line 56). Wrap the analysis block in a `space-y-4` container. Do NOT alter the header staleness hint (lines 246-261), any other TabsContent, or the existing Overview Card. Do NOT introduce "use client" in the page (it is a Server Component; all rendered children are server-safe).</action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -q "OverviewAnalysis" "app/(app)/companies/[id]/page.tsx"</automated>
  </verify>
  <done>Overview tab renders the existing Card unchanged plus OverviewAnalysis (or DeepDiveEmpty when no analysis); tsc + eslint clean; no other tab or the header changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Overview tab now renders the full investment thesis from the stored deep-dive: Executive Summary pinned top, analytical sections as collapsibles with Fact/Estimate + confidence chips and 1-10 rating indicators (moat + per-dimension strategic moat), TAM/SAM/SOM directional ranges, and IC Conclusion pinned bottom with a rating badge. Plan 01's regen script produces the fuller section set.</what-built>
  <how-to-verify>
    1. Regenerate a demo company's deep-dive so it has the fuller sections: `node --conditions=react-server --import tsx scripts/regen-deep-dive.ts "Replit"` — confirm the printed section-key list includes strategic_moat, market_opportunity, product_portfolio, unit_economics, historical_analogue, and an ic_conclusion rating.
    2. `npm run dev`, open the demo company page, Overview tab.
    3. Confirm: Executive Summary is pinned at the top; the analytical sections (Technology w/ moat rating, Product Portfolio, Vertical & Customer, Business Model, Unit Economics, Market Opportunity TAM/SAM/SOM, Strategic Moat w/ 4 dimension ratings, Historical Analogue, Outlook & Exit) render as collapsibles; IC Conclusion is pinned at the bottom with a rating badge.
    4. Confirm every forward-looking field shows a Fact/Estimate + Low/Med/High chip, and Outlook & Exit shows NO probabilities or price targets.
    5. Confirm the existing Overview stats/founders/description/BusinessModelAnalysis are still present and unchanged, and other tabs are untouched.
    6. On a company with no analysis, confirm the Overview tab shows the "Run deep-dive" empty-state CTA (not empty shells).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what renders wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored analysis → rendered UI | `sections` JSONB (originally Grok-generated) is rendered to the browser; React escaping and typed access are the safety layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering (XSS) | overview-sections rendering of `sections.*.text` | mitigate | Render all narrative via JSX text children only; NO `dangerouslySetInnerHTML`. React auto-escapes. Verified by grep gate absence of dangerouslySetInnerHTML. |
| T-02-05 | Information Disclosure | fabricated probabilities/price targets surfacing | mitigate | Renderer never maps a probability/price row; Outlook & Exit renders narrative labelled fields only. Plan 01 normalizeSections already strips such keys. |
| T-02-06 | Denial of Service | undefined section fields crashing the RSC render | mitigate | All fields optional; renderer degrades absent fields to SectionEmpty/omission, never throws on undefined. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (reuses existing lucide/Radix/Badge); no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run lint` clean.
- `grep -L "dangerouslySetInnerHTML" components/company/overview-sections.tsx` confirms no raw HTML injection.
- Human checkpoint: full Overview render verified against a regenerated demo company (see checkpoint steps).
</verification>

<success_criteria>
- Executive Summary pinned top, IC Conclusion (rating + bull/bear + recommendation) pinned bottom (OVR-01, OVR-04).
- Technology moat rating 1-10 + Product Portfolio + Vertical/Customer + enriched Business Model render (OVR-02).
- Unit Economics + TAM/SAM/SOM ranges + per-dimension Strategic Moat 1-10 + Historical Analogue render (OVR-03).
- Outlook & Exit narrative with no probabilities/price targets; every forward-looking field carries its ConfidenceChip (OVR-04).
- All added sections use CollapsibleSection/SectionEmpty; DeepDiveEmpty shows before first run; existing Overview content preserved (OVR-05).
</success_criteria>

<output>
Create `.planning/phases/02-overview-enrichment/02-02-SUMMARY.md` when done.
</output>
