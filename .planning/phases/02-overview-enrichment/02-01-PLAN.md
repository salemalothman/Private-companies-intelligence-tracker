---
phase: 02-overview-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/agents/deep-dive-types.ts
  - lib/agents/deep-dive.ts
  - lib/agents/deep-dive.test.ts
  - scripts/regen-deep-dive.ts
  - docs/superpowers/specs/2026-07-02-company-deep-dive-analysis-design.md
autonomous: true
requirements: [OVR-01, OVR-02, OVR-03, OVR-04]
must_haves:
  truths:
    - "A re-run deep-dive produces a sections object containing all OVR section keys (executive_summary, technology, product_portfolio, vertical_customer, business_model, unit_economics, market_opportunity, strategic_moat, historical_analogue, outlook_and_exit, ic_conclusion)."
    - "Core Technology carries a moat_rating integer 1-10; Strategic Moat carries per-dimension integers 1-10 (switching_costs, network_flywheel, distribution_regulatory, ip)."
    - "TAM/SAM/SOM render as directional labelled fields, not asserted exact figures."
    - "Outlook & Exit contains no probability or price-target fields; IC Conclusion carries a rating enum plus bull/bear/recommendation."
    - "Growth comps input and valuation JSONB shape are unchanged from Phase 1."
  artifacts:
    - path: "lib/agents/deep-dive-types.ts"
      provides: "Per-section typed AnalysisSections shape (source of truth)"
      contains: "OverviewSections"
    - path: "lib/agents/deep-dive.ts"
      provides: "Extended Grok prompt + zod schema emitting full section set"
      contains: "strategic_moat"
    - path: "scripts/regen-deep-dive.ts"
      provides: "Service-role script to regenerate one company's deep-dive end-to-end"
    - path: "lib/agents/deep-dive.test.ts"
      provides: "Unit tests for section-normalization mapping and unchanged comps helpers"
  key_links:
    - from: "lib/agents/deep-dive.ts"
      to: "lib/agents/deep-dive-types.ts"
      via: "imports OverviewSections / AnalysisSections typed shape"
      pattern: "OverviewSections|AnalysisSections"
    - from: "lib/agents/deep-dive.ts"
      to: "analysisSchema (zod)"
      via: "zod schema validates the full section set"
      pattern: "strategic_moat|moat_rating|ic_conclusion"
---

<objective>
Tighten `AnalysisSections` into per-section typed shapes and extend `runDeepDive`'s Grok prompt + zod output schema so a deep-dive run emits the FULL OVR section set (executive summary through IC conclusion, with numeric moat ratings, per-dimension strategic-moat scores, TAM/SAM/SOM directional ranges, and a rated IC conclusion). Add a regeneration script and unit tests, and update spec §3 (types are source of truth).

Purpose: Phase 1 stored only a partial 7-section set; OVR-02/OVR-03/OVR-04 require the fuller, granular set. This plan is the data side of the vertical slice — Plan 02 renders it. Without this, the Overview tab has nothing to render.

Output: Typed section contracts, an extended agent that produces them, a script that regenerates a real company end-to-end, and green unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-overview-enrichment/02-CONTEXT.md
@docs/superpowers/specs/2026-07-02-company-deep-dive-analysis-design.md

<interfaces>
<!-- Existing contracts the executor implements against. Do NOT re-derive. -->

From lib/agents/deep-dive-types.ts (current — to be tightened):
- `LabelledField { text: string; basis: "fact" | "estimate"; confidence: "low" | "med" | "high"; source?: string }` — KEEP AS-IS. Analysis-layer confidence is "med" (not "medium").
- `AnalysisSections { [section: string]: AnalysisSectionValue }` — currently OPEN; tighten to a typed `OverviewSections` shape while keeping `AnalysisSections` as the stored alias for backward-compatibility with existing rows.
- `AnalysisValuation` (base_revenue, current_valuation, peer_multiple, growth) — DO NOT CHANGE. Comps stay code-computed.

From lib/agents/deep-dive.ts:
- `analysisSchema = z.object({ sections: z.record(z.string(), z.unknown()).nullish(), growth: z.object({...}).nullish() })` — the `sections` half is what this plan tightens; the `growth` half stays untouched.
- `ANALYSIS_SHAPE` (string) and `buildPrompt(grounding)` — the prompt describing the JSON shape to Grok; extend both to describe the full section set.
- `computePeerMultiple`, `deriveBaseRevenue`, `percentile` — code-computed comps helpers; DO NOT CHANGE (they have passing tests).
- `runDeepDive(supabase, company)` — step 2 parses sections; extend parsing/normalization to the typed shape, degrading missing fields gracefully (never throw).

From lib/types.ts:
- `CompanyAnalysisRow.sections: AnalysisSections` and `.valuation: AnalysisValuation` — the stored column types. Keeping `AnalysisSections` as an alias means this file needs no change.

From scripts/sync-company.ts (mirror this script harness):
- Loads `.env.local` via `process.loadEnvFile`, polyfills `ws` WebSocket, creates a service-role Supabase client, looks up a company by name via `.ilike`, runs a server-side agent. Copy this structure for the regen script.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten AnalysisSections into typed per-section OverviewSections shape</name>
  <read_first>
    - lib/agents/deep-dive-types.ts (full — the shape to tighten; keep LabelledField and AnalysisValuation unchanged)
    - .planning/phases/02-overview-enrichment/02-CONTEXT.md (§decisions — the exact section set and per-section fields)
    - docs/superpowers/specs/2026-07-02-company-deep-dive-analysis-design.md (§2 Overview list, §3 field shape)
    - lib/types.ts lines 401-410 (CompanyAnalysisRow uses AnalysisSections — keep the alias so this file is untouched)
  </read_first>
  <behavior>
    - A `Rating1to10` helper type constrains numeric ratings; a runtime clamp helper `clampRating(n)` returns an integer in 1..10 (or null for non-finite / out-of-domain input) so downstream numbers are never fabricated beyond the domain.
    - `OverviewSections` names every OVR section key with its typed shape: executive_summary (thesis, value_prop, strengths[], weaknesses[], positioning, most_likely_outcome as LabelledFields/arrays); technology (LabelledField + moat_rating: Rating1to10|null); product_portfolio; vertical_customer; business_model; unit_economics; market_opportunity (tam/sam/som each a LabelledField for directional range + confidence); strategic_moat (per-dimension switching_costs/network_flywheel/distribution_regulatory/ip each Rating1to10|null, plus optional narrative LabelledField); historical_analogue; outlook_and_exit (narrative LabelledField(s) only — NO probability/price-target fields); ic_conclusion (rating enum "strong_buy"|"buy"|"hold"|"sell", bull LabelledField, bear LabelledField, recommendation LabelledField).
    - Every field is optional at the type level so a partial or older stored row still type-checks (the renderer in Plan 02 handles absence via SectionEmpty).
    - `AnalysisSections` remains exported as an alias of `OverviewSections` (widened to keep the open index for round-tripping legacy rows) so lib/types.ts and existing rows need no migration.
  </behavior>
  <action>Extend lib/agents/deep-dive-types.ts. Add a `Rating1to10` type and an exported pure `clampRating(n: number | null | undefined): number | null` helper (integer 1..10, null otherwise) with a JSDoc explaining why we never fabricate a rating outside the domain. Define the exported `OverviewSections` interface with the per-section typed shapes named in the behavior block, using the EXISTING `LabelledField` for all narrative fields (basis + confidence + optional source) and `Rating1to10 | null` for numeric ratings. Keep `LabelledField` and `AnalysisValuation` exactly as-is. Redefine `AnalysisSections` so it is compatible with both `OverviewSections` and legacy open rows (intersection of the typed shape with the existing open index signature) — the stored alias used by CompanyAnalysisRow must still accept the Phase-1 partial rows. Update the file-level and per-section JSDoc to state this is now the tightened Phase-2 source of truth. Do NOT add moat_rating/strategic_moat outside the LabelledField honesty model — ratings are numeric indicators, narrative stays labelled. Add the unit tests for `clampRating` to lib/agents/deep-dive.test.ts in this task (RED first): clampRating(5)===5, clampRating(0)===null, clampRating(11)===null, clampRating(7.8)===7, clampRating(null)===null, clampRating(NaN)===null.</action>
  <verify>
    <automated>npx vitest run lib/agents/deep-dive.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>OverviewSections + Rating1to10 + clampRating exported from deep-dive-types.ts; clampRating tests pass; tsc clean with lib/types.ts unchanged (AnalysisSections alias still assignable to CompanyAnalysisRow.sections).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend runDeepDive prompt + zod schema to emit the full section set + normalization</name>
  <read_first>
    - lib/agents/deep-dive.ts (full — analysisSchema, ANALYSIS_SHAPE, buildPrompt, runDeepDive step 2; comps helpers stay untouched)
    - lib/agents/deep-dive-types.ts (the OverviewSections shape from Task 1)
    - lib/agents/deep-dive.test.ts (existing computePeerMultiple/deriveBaseRevenue tests — must stay green; add section-normalization tests here)
  </read_first>
  <behavior>
    - A pure exported `normalizeSections(raw: unknown): OverviewSections` maps a parsed Grok object into the typed shape: numeric ratings pass through `clampRating` (moat_rating and each strategic_moat dimension), narrative fields keep {text, basis, confidence, source}, unknown/missing keys are dropped, ic_conclusion.rating is coerced to the enum or dropped if not one of the four values.
    - normalizeSections never throws and returns `{}` for null/undefined/non-object input.
    - normalizeSections strips any probability/price-target-looking keys from outlook_and_exit (defense-in-depth for the guardrail) — a raw outlook object with an extra `probability` key yields an outlook_and_exit without it.
    - The zod `analysisSchema.sections` half accepts the full nested object (still `.nullish()` and permissive at the parse boundary; normalization does the shaping).
  </behavior>
  <action>Edit lib/agents/deep-dive.ts. (1) Add exported pure `normalizeSections(raw: unknown): OverviewSections` implementing the behavior above, importing `clampRating`, `OverviewSections`, `LabelledField` from deep-dive-types. Keep it dependency-free and near the top with other pure helpers so it is unit-testable in the node-only Vitest env. (2) Rewrite `ANALYSIS_SHAPE` to describe the full section set to Grok: executive_summary{thesis,value_prop,strengths[],weaknesses[],positioning,most_likely_outcome} each a labelled field or array of labelled fields; technology{...labelled..., moat_rating: integer 1-10}; product_portfolio; vertical_customer; business_model; unit_economics; market_opportunity{tam,sam,som each a labelled directional range}; strategic_moat{switching_costs,network_flywheel,distribution_regulatory,ip each integer 1-10, plus optional narrative}; historical_analogue; outlook_and_exit{narrative labelled fields only}; ic_conclusion{rating:"strong_buy"|"buy"|"hold"|"sell", bull, bear, recommendation}. (3) Extend `buildPrompt` prose to enumerate these sections and REPEAT the hard rules already present verbatim: no probabilities/probability tables, no price targets, no exact valuation/financial figures asserted as fact, ratings are 1-10 integers reflecting qualitative judgement (not fabricated financials), numbers appear ONLY inside growth (rates) and the 1-10 rating indicators. (4) In `runDeepDive` step 2, replace `sections = (parsed.data.sections ?? {}) as AnalysisSections` with `sections = normalizeSections(parsed.data.sections)`. Do NOT touch the growth parsing, the comps `computePeerMultiple`/`deriveBaseRevenue` calls, the valuation object, or the upsert. Behavior on Grok failure stays: empty analysis, still a timestamped upsert. Add section-normalization unit tests to lib/agents/deep-dive.test.ts (RED first): full raw object → all keys typed + ratings clamped; ic rating "invalid" dropped; outlook with a probability key stripped; null input → {}; moat_rating 12 → null.</action>
  <verify>
    <automated>npx vitest run lib/agents/deep-dive.test.ts && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>normalizeSections exported and tested; ANALYSIS_SHAPE + buildPrompt describe the full section set with the hard-rule guardrail repeated; runDeepDive step 2 uses normalizeSections; existing comps tests still green; tsc + eslint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add regeneration script + update spec §3</name>
  <read_first>
    - scripts/sync-company.ts (full — the service-role script harness to mirror: env load, ws polyfill, service-role client, ilike lookup)
    - lib/agents/deep-dive.ts (runDeepDive signature + CompanyWithRelations shape it needs)
    - lib/queries.ts lines 158-175 (getCompanyAnalysis reader, to print the result)
    - docs/superpowers/specs/2026-07-02-company-deep-dive-analysis-design.md (§3 — the field-shape block to update)
  </read_first>
  <action>Create scripts/regen-deep-dive.ts mirroring scripts/sync-company.ts exactly (ws polyfill, `process.loadEnvFile(".env.local")`, service-role Supabase client, `NAME = process.argv[2] ?? "Replit"`, header doc comment with the `node --conditions=react-server --import tsx scripts/regen-deep-dive.ts "<Name>"` invocation line). Load the full CompanyWithRelations for the matched company using the same query shape getCompany uses (import from lib/queries or replicate the select with valuations/funding_rounds/news/investments joins the agent reads), call `runDeepDive(sb, company)`, then re-read `company_analysis` for that company and console.log the top-level section keys present and the ic_conclusion.rating so the operator can confirm the fuller set was produced. Degrade gracefully and exit non-zero on error. Then update docs/superpowers/specs/2026-07-02-company-deep-dive-analysis-design.md §3: replace the single generic field-shape jsonc block with the tightened per-section list (mirror OverviewSections: which sections exist, which carry a moat_rating / per-dimension 1-10 ratings / TAM-SAM-SOM ranges / ic rating enum), and add a one-line note "Types in lib/agents/deep-dive-types.ts are the source of truth." Do NOT change §4 (valuation) — comps shape is unchanged.</action>
  <verify>
    <automated>npx tsc --noEmit && test -f scripts/regen-deep-dive.ts && grep -q "runDeepDive" scripts/regen-deep-dive.ts && grep -q "source of truth" docs/superpowers/specs/2026-07-02-company-deep-dive-analysis-design.md</automated>
  </verify>
  <done>scripts/regen-deep-dive.ts exists, mirrors the sync-company harness, calls runDeepDive and prints the produced section keys + ic rating; spec §3 updated to the per-section shape with the source-of-truth note; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grok model → app (LLM output) | Untrusted generated JSON crosses into stored `sections`; must be parsed, normalized, and guardrail-filtered before persistence. |
| script → Supabase (service role) | regen-deep-dive.ts uses the service-role client (bypasses RLS); operator-run only, secrets from `.env.local`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Grok `sections` output | mitigate | normalizeSections drops unknown keys, clamps ratings to 1-10, coerces ic rating to the enum, and strips probability/price-target keys from outlook_and_exit before upsert. |
| T-02-02 | Information Disclosure | fabricated financials in narrative | mitigate | buildPrompt repeats hard rules (no probabilities, no price targets, no exact figures as fact); narrative stays under the LabelledField basis/confidence honesty model. |
| T-02-03 | Elevation of Privilege | regen script service-role client | accept | Operator-run local/CI script only; not reachable from the web runtime; secrets stay in `.env.local` (gitignored). Mirrors the accepted scripts/sync-company.ts pattern. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (all deps already present); no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run lib/agents/deep-dive.test.ts` — clampRating + normalizeSections + existing comps tests all green.
- `npx tsc --noEmit` — clean; lib/types.ts unchanged and still assignable.
- `npm run lint` — clean.
- Manual (operator): `node --conditions=react-server --import tsx scripts/regen-deep-dive.ts "Replit"` prints the full section-key list including strategic_moat, market_opportunity, product_portfolio, unit_economics, historical_analogue, and an ic_conclusion.rating.
</verification>

<success_criteria>
- OverviewSections typed shape covers every OVR-01..04 section with numeric ratings (moat 1-10, per-dimension strategic-moat 1-10) and TAM/SAM/SOM directional ranges.
- runDeepDive emits and normalizes the full set; guardrail (no probabilities/price targets) enforced in prompt AND normalization.
- Comps/valuation shape unchanged; existing tests green; tsc + eslint clean.
- Regeneration script produces the fuller set for a real demo company.
</success_criteria>

<output>
Create `.planning/phases/02-overview-enrichment/02-01-SUMMARY.md` when done.
</output>
