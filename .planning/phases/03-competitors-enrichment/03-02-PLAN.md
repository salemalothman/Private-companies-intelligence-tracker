---
phase: 03-competitors-enrichment
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/company/competitors-analysis.tsx
  - app/(app)/companies/[id]/page.tsx
  - scripts/regen-deep-dive.ts
autonomous: false
requirements: [CMP-01, CMP-02]

must_haves:
  truths:
    - "A user viewing the Competitors tab after a deep-dive sees the ranking rows grouped by threat tier (direct → indirect → emerging → unclassified) with all existing columns and behavior preserved."
    - "A Capability Matrix grid renders the target + its top 3 threats scored 1-10 across IP Depth, GTM Velocity, Capital Efficiency, and Workflow Retention using RatingIndicator."
    - "Before the first deep-dive run the enrichment shows the DeepDiveEmpty CTA while the raw ranking table still renders as today."
  artifacts:
    - path: "components/company/competitors-analysis.tsx"
      provides: "Server-component renderer: tier-grouped ranking table + Capability Matrix grid"
      min_lines: 80
      contains: "CompetitorsAnalysis"
    - path: "app/(app)/companies/[id]/page.tsx"
      provides: "Competitors tab wired to CompetitorsAnalysis with analysis + ranking + DeepDiveEmpty"
      contains: "CompetitorsAnalysis"
  key_links:
    - from: "app/(app)/companies/[id]/page.tsx Competitors tab"
      to: "components/company/competitors-analysis.tsx"
      via: "props: competitorRanking + analysis.sections.competitors"
      pattern: "CompetitorsAnalysis"
    - from: "components/company/competitors-analysis.tsx"
      to: "components/company/rating-indicator.tsx"
      via: "matrix cells"
      pattern: "RatingIndicator"
---

<objective>
Render the Phase 3 competitor enrichment in the Competitors tab: group the
EXISTING ranking rows by threat tier (CMP-01) and render a Capability Matrix grid
of the target vs. its top 3 threats using the existing RatingIndicator (CMP-02).
Join the stored `competitors` block onto the ranking rows by name
(case-insensitive) — NO re-discovery. Preserve every existing column and
behavior. Show DeepDiveEmpty before the first run.

Purpose: Deliver the user-visible outcome of Phase 3, mirroring the Phase 2
Overview renderer pattern (server component, RSC-safe reuse of RatingIndicator /
CollapsibleSection / DeepDiveEmpty).

Output: a new `components/company/competitors-analysis.tsx` server component, the
Competitors tab wired to it, and an extended regen script that prints the produced
competitors keys for the end-to-end verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-competitors-enrichment/03-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/03-competitors-enrichment/03-01-SUMMARY.md

@components/company/overview-sections.tsx
@components/company/rating-indicator.tsx
@components/company/confidence-chip.tsx

<interfaces>
<!-- Contracts the renderer consumes. Types come from plan 03-01. -->

From lib/agents/deep-dive-types.ts (produced by 03-01):
```typescript
export type ThreatTier = "direct" | "indirect" | "emerging";
export interface CapabilityThreat {
  name: string;
  ip_depth: Rating1to10 | null;
  gtm_velocity: Rating1to10 | null;
  capital_efficiency: Rating1to10 | null;
  workflow_retention: Rating1to10 | null;
}
export interface CapabilityMatrix { target: string; threats: CapabilityThreat[]; }
export interface CompetitorsSection {
  threat_tiers?: Record<string, ThreatTier>;
  capability_matrix?: CapabilityMatrix;
  narrative?: LabelledField;
}
// OverviewSections.competitors?: CompetitorsSection
```

From lib/competitors/rank.ts — the rows to group (keyed by `name`):
```typescript
export interface RankedEntity {
  name: string; valuation: number | null; valuationDate: string | null;
  revenue: number | null; multiple: number | null; basis: string | null;
  source: string | null; secVerified: boolean; isTarget: boolean;
}
```

Reusable presentational primitives (all server-safe, NO "use client"):
```typescript
// components/company/rating-indicator.tsx
export function RatingIndicator({ label, value, className }: { label: string; value: Rating1to10 | null | undefined; className?: string; }): JSX.Element;
// components/company/confidence-chip.tsx
export function ConfidenceChip({ basis, confidence, className }): JSX.Element;
export function DeepDiveEmpty({ message?, action?, className? }): JSX.Element;
// components/dashboard/collapsible-section.tsx — MUST be used icon-less from a Server Component (RSC boundary)
export function CollapsibleSection({ title, defaultOpen?, children }): JSX.Element;
export function SectionEmpty({ children }): JSX.Element;
```
</interfaces>

<!-- The existing Competitors-tab table lives in app/(app)/companies/[id]/page.tsx
     around lines 568-686. Its columns, target highlight, source link, basis
     tooltip, and SEC-verified badge MUST be preserved verbatim. The page already
     builds `competitorRanking` (RankedEntity[]), `peers`, `analysis`, and imports
     DeepDiveButton + DeepDiveEmpty + formatCurrency + formatMultiple +
     formatDate + isPublisherDomain + ShieldCheck + cn. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the CompetitorsAnalysis server component (tier grouping + Capability Matrix)</name>
  <read_first>
    - components/company/overview-sections.tsx (server-component patterns to mirror: Field/ConfidenceChip usage, RatingIndicator grid, icon-less CollapsibleSection, RSC-safe comment header, PANEL_BODY)
    - components/company/rating-indicator.tsx (matrix cell)
    - components/company/confidence-chip.tsx (DeepDiveEmpty, ConfidenceChip)
    - app/(app)/companies/[id]/page.tsx lines ~568-686 (the exact existing ranking table markup — columns, target highlight classes, source link, basis tooltip, SEC-verified badge — to reproduce inside the grouped table)
    - lib/agents/deep-dive-types.ts (CompetitorsSection + RankedEntity via lib/competitors/rank.ts)
    - .planning/phases/03-competitors-enrichment/03-CONTEXT.md (§Rendering: tier order, Unclassified group, matrix as RatingIndicator grid, design-system flat/hairline/tabular-nums)
  </read_first>
  <files>components/company/competitors-analysis.tsx</files>
  <action>
    Create components/company/competitors-analysis.tsx as a Server Component (NO
    "use client" — copy the RSC-safe header-comment rationale from
    overview-sections.tsx). Export `CompetitorsAnalysis({ ranking, competitors }:
    { ranking: RankedEntity[]; competitors: CompetitorsSection | undefined })`.

    Threat-tier grouping (CMP-01): build a case-insensitive lookup from
    `competitors?.threat_tiers` (lowercased name → tier). Partition `ranking` into
    ordered groups: `direct`, `indirect`, `emerging`, then `unclassified` (rows
    whose name has no tier — the target row, which is never classified, falls
    here). Render each non-empty group as a section with a small `label-eyebrow`
    heading ("Direct threats" / "Indirect / asymmetric" / "Emerging / stealth" /
    "Unclassified") followed by a table that reproduces the EXISTING ranking
    columns and behavior VERBATIM from page.tsx: rank # (use the row's global
    ranking index so numbering stays continuous across groups), Company (target
    highlight via `e.isTarget && "bg-primary/[0.07] font-bold ..."` and the
    left-border accent), Latest valuation (`formatCurrency`), Revenue/ARR, V/R
    multiple (`formatMultiple`), As of (`formatDate`), Source (publisher link via
    `isPublisherDomain`), Basis (truncate + title tooltip), Verified (ShieldCheck
    + "SEC" for secVerified non-target, em-dash otherwise). Keep tabular-nums on
    every figure. Import Table primitives from @/components/ui/table and the
    formatters/`isPublisherDomain`/`cn` from the same modules page.tsx uses.

    Capability Matrix (CMP-02): from `competitors?.capability_matrix`, render a
    compact grid — one `RatingIndicator` block per (row, dimension). Rows = the
    matrix `target` plus each `threat` (already capped at 3 by normalize);
    columns = IP Depth, GTM Velocity, Capital Efficiency, Workflow Retention. For
    the target row, all four scores are absent → RatingIndicator renders the
    em-dash "no rating" state (target has no self-scores in the block); for each
    threat row pass `threat.ip_depth` etc. Lay it out as a labelled grid
    (per-threat card with a name heading and a 4-up RatingIndicator grid, matching
    the StrategicMoatPanel `grid gap-4 sm:grid-cols-2` aesthetic) so it visually
    matches the Overview moat ratings. Render an optional `competitors.narrative`
    via a Field-style block with ConfidenceChip (mirror overview-sections `Field`).

    Empty/degrade: if `competitors` is undefined or has neither tiers nor a
    matrix, the component renders ONLY the flat ranking table (no grouping) so the
    tab is behavior-preserving before a run — the page-level DeepDiveEmpty CTA
    (Task 2) sits above it. If `capability_matrix` is absent, omit the matrix
    section. Every field optional; absence degrades, never crashes. All narrative
    text rendered as JSX children only (React auto-escapes; NO
    dangerouslySetInnerHTML). Do NOT add fenced code inside this action.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>tsc + eslint clean; CompetitorsAnalysis exported from a server component; tier groups render in direct→indirect→emerging→unclassified order with all existing columns; matrix renders RatingIndicator cells for the four dimensions; no "use client", no dangerouslySetInnerHTML.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CompetitorsAnalysis into the Competitors tab + extend regen print</name>
  <read_first>
    - app/(app)/companies/[id]/page.tsx (Competitors TabsContent ~568-686; the Overview tab wiring ~379-387 that gates OverviewAnalysis vs DeepDiveEmpty on `analysis` — mirror that gate; existing imports for DeepDiveButton/DeepDiveEmpty)
    - components/company/competitors-analysis.tsx (from Task 1)
    - scripts/regen-deep-dive.ts (the section-key print block to extend)
  </read_first>
  <files>app/(app)/companies/[id]/page.tsx, scripts/regen-deep-dive.ts</files>
  <action>
    In app/(app)/companies/[id]/page.tsx: import `CompetitorsAnalysis` from
    @/components/company/competitors-analysis. In the Competitors `TabsContent`
    (value="competitors"), keep the existing header (title + description +
    RefreshCompetitorsButton) and the `peers.length === 0` EmptyRow branch. In the
    non-empty branch, REPLACE the inline `<Table>…competitorRanking.map…</Table>`
    block with `<CompetitorsAnalysis ranking={competitorRanking} competitors={(analysis?.sections
    as OverviewSections | undefined)?.competitors} />` (narrow the stored sections
    to OverviewSections like the Overview tab does; import the type if not already
    imported). Above the table, when `!analysis`, render `<DeepDiveEmpty action={<DeepDiveButton companyId={company.id} />} />`
    (mirror the Overview tab gate at ~379-387) so the CTA shows before the first
    run while the flat ranking still renders below it. Remove any now-unused
    imports IF and ONLY IF nothing else in the file uses them (the ranking-table
    formatters are still used elsewhere — verify before removing; prefer leaving
    imports rather than breaking other tabs).

    In scripts/regen-deep-dive.ts: after the existing `ic_conclusion.rating`
    print, add lines that read `sections.competitors` and print the tier count
    (`Object.keys(threat_tiers ?? {}).length`) and the matrix threat count
    (`capability_matrix?.threats?.length ?? 0`) so the operator can confirm the
    competitors block landed end-to-end. Do NOT add fenced code inside this
    action.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>tsc + eslint clean; next build succeeds; Competitors tab renders CompetitorsAnalysis with tiers + matrix when analysis exists and DeepDiveEmpty before first run; existing ranking columns/behavior preserved for all other tabs; regen script prints competitors tier + matrix counts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — regen Replit + live browser check of tiers + matrix</name>
  <action>Orchestrator-run only: regenerate one demo company (Replit) via the service-role script, then do the live browser check per how-to-verify below. No autonomous code changes in this task.</action>
  <what-built>
    Extended runDeepDive competitors block (plan 03-01) + the Competitors-tab
    renderer (tier grouping + Capability Matrix). The ORCHESTRATOR runs the
    service-role regeneration for one demo company and does the live browser check.
  </what-built>
  <how-to-verify>
    1. Regenerate Replit end-to-end (service-role, reads .env.local):
       `node --conditions=react-server --import tsx scripts/regen-deep-dive.ts "Replit"`
       Expect the print to include the competitors block: a non-zero tier count
       and a matrix threat count of 1-3 (Replit's ranked competitors per the
       earlier run include Cursor, Cognition, Lovable).
    2. Start the app, open the Replit company page → Competitors tab.
    3. Confirm the ranking rows are grouped under tier headings (direct →
       indirect → emerging → unclassified), the target Replit row is highlighted,
       and every existing column is intact (rank #, latest valuation, revenue,
       V/R multiple, as-of, source link, basis, SEC-verified badge).
    4. Confirm the Capability Matrix grid shows Replit + its top 3 threats scored
       1-10 across IP Depth, GTM Velocity, Capital Efficiency, Workflow Retention
       via the same RatingIndicator bars used on the Overview moat ratings.
    5. Confirm a company with NO analysis shows the "Run deep-dive" DeepDiveEmpty
       CTA above the (still-rendered) flat ranking table.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the rendering/tier/matrix issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored analysis JSONB → RSC render | Previously-normalized (03-01) competitors block rendered in the page |
| Grok narrative text → DOM | competitors.narrative rendered as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering (XSS) | competitors-analysis.tsx narrative/name render | mitigate | Render all Grok-originated text (narrative, competitor names) as JSX children only; NO dangerouslySetInnerHTML — React auto-escapes |
| T-03-04 | Information disclosure | Competitors tab | accept | Page is already owner-gated by RLS on the company row (Phase 1); no new data exposure |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages; all primitives (RatingIndicator, ConfidenceChip, Table) already present |
</threat_model>

<verification>
- `npx tsc --noEmit` — clean.
- `npm run lint` — clean.
- `npx next build` — succeeds.
- Orchestrator regen + live browser check (checkpoint task 3) confirms tiers + matrix render and existing table behavior is preserved.
</verification>

<success_criteria>
- Competitors tab groups the existing ranking by threat tier (CMP-01), preserving all columns + target highlight + source link + basis + SEC-verified badge.
- Capability Matrix grid shows the target + top 3 threats scored 1-10 across the four dimensions via RatingIndicator (CMP-02).
- DeepDiveEmpty CTA shows before the first run; the flat ranking still renders.
- No re-discovery (join by name only); no XSS (no dangerouslySetInnerHTML); server-component render.
</success_criteria>

<output>
Create `.planning/phases/03-competitors-enrichment/03-02-SUMMARY.md` when done.
</output>
