---
phase: 05-valuation-targets-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/valuation/comps.ts
  - lib/valuation/comps.test.ts
autonomous: true
requirements: [VAL-02, VAL-03, VAL-04]
tags: [valuation, comps]

must_haves:
  truths:
    - "projected_revenue(n) = base_revenue × (1+growth)^n is computed in code for years 2026–2030"
    - "implied_valuation = projected_revenue × applied_multiple is computed in code for every cell"
    - "Bear/Base/Bull presets pair growth.bear/base/bull with peer_multiple.p25/median/p75"
    - "A null base_revenue or null applied multiple propagates to a null cell (never 0)"
    - "User growth/multiple overrides recompute the same table via the same pure function"
  artifacts:
    - path: "lib/valuation/comps.ts"
      provides: "Pure comps table builder + scenario/override types consumed by the client tab"
      exports: ["buildCompsTable", "CompsInputs", "CompsOverrides", "CompsRow", "ScenarioKey", "COMPS_YEARS", "clampGrowth"]
      min_lines: 60
    - path: "lib/valuation/comps.test.ts"
      provides: "Unit tests: math correctness, null propagation, override clamping, scenario mapping"
      min_lines: 60
  key_links:
    - from: "lib/valuation/comps.ts"
      to: "lib/agents/deep-dive-types.ts (AnalysisValuation)"
      via: "type import — reads base_revenue.value, peer_multiple.{p25,median,p75}, growth.{bear,base,bull}"
      pattern: "AnalysisValuation"
---

<objective>
Build the pure, code-computed comps model that produces the Valuation Targets
table — the ONLY source of $ figures in Phase 5. It maps a stored
`AnalysisValuation` (unchanged) plus optional user overrides into a table of
{year, bear, base, bull} implied-valuation rows for 2026–2030, with honest null
propagation and override clamping. No React, no I/O — a directly unit-testable
module (VAL-02, VAL-03) whose output the Wave-2 client tab recomputes live
(VAL-04).

Purpose: All valuation math lives in one deterministic, tested place so the tab
never asserts a fabricated forecast — only transparent peer-comparable math.
Output: `lib/valuation/comps.ts` + `lib/valuation/comps.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-valuation-targets-tab/05-CONTEXT.md

<read_first>
- docs/superpowers/specs/2026-07-02-company-deep-dive-analysis-design.md — §4
  (the comps math + scenarios: `projected revenue(year) = base revenue ×
  (1 + growth)^n`; `implied valuation = projected revenue × applied V/R
  multiple`; Bear=p25, Base=median, Bull=p75).
- lib/agents/deep-dive-types.ts — `AnalysisValuation` (READ-ONLY input contract):
  `base_revenue.value: number | null`, `peer_multiple.{median,p25,p75}: number |
  null`, `growth.{base,bear,bull}: number`. DO NOT edit this file in this plan.
- lib/agents/deep-dive.ts — the `percentile` helper + `computePeerMultiple`
  ordering (p25/median/p75) so the scenario→percentile mapping matches producer
  semantics. DO NOT edit; read for the contract only.
- lib/agents/deep-dive.test.ts (lines 1–60) — the Vitest import/`describe`/`it`
  style to mirror (co-located `.test.ts`, `expect().toBe()` / `.toBeNull()`).
</read_first>

<interfaces>
From lib/agents/deep-dive-types.ts (input contract — do not modify):

  interface AnalysisValuation {
    base_revenue: { value: number | null; source: string | null };
    current_valuation: number | null;
    peer_multiple: {
      median: number | null; p25: number | null; p75: number | null;
      n_peers: number; n_sec_verified: number;
    };
    growth: {
      base: number; bear: number; bull: number;
      confidence: "low" | "med" | "high"; rationale: string;
    };
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for the pure comps table builder</name>
  <files>lib/valuation/comps.test.ts</files>
  <read_first>
    - lib/agents/deep-dive.test.ts (lines 1–60) — Vitest style to mirror.
    - docs/superpowers/specs/2026-07-02-company-deep-dive-analysis-design.md §4 — math.
  </read_first>
  <behavior>
    - COMPS_YEARS equals exactly [2026, 2027, 2028, 2029, 2030] (base year 2026, n = year − 2026 so 2026 exponent is 0).
    - buildCompsTable with base_revenue.value=240_000_000, peer_multiple {p25:13.5, median:14.5, p75:15.5}, growth {bear:0.25, base:0.45, bull:0.70} returns 5 rows keyed by year; for 2026 (n=0): base cell = 240e6 × 14.5 = 3.48e9; bull 2026 = 240e6 × 15.5 = 3.72e9; bear 2026 = 240e6 × 13.5 = 3.24e9.
    - For 2027 (n=1) base cell = 240e6 × (1+0.45)^1 × 14.5 = 5.046e9 (assert via toBeCloseTo).
    - Scenario mapping: bear uses growth.bear × peer_multiple.p25; base uses growth.base × median; bull uses growth.bull × p75. A test asserts each column uses its paired percentile (e.g. swapping only p75 changes bull but not bear/base).
    - Null propagation: base_revenue.value=null → every cell in every row is null (never 0). peer_multiple.median=null → only the base column is null; bear/bull still compute. Assert cells are strictly `null`, not `0`.
    - clampGrowth clamps to the documented range (−0.5 .. 3.0 = −50%..+300%): clampGrowth(-2) === -0.5, clampGrowth(5) === 3.0, clampGrowth(0.45) === 0.45, clampGrowth(NaN) and clampGrowth(null) return null.
    - Overrides: buildCompsTable(inputs, { growth: 0.10, multiplePercentile: "p25" }) recomputes ALL three columns from the single overridden growth 0.10 and the p25 multiple (assert the base 2026 cell = base_revenue × p25 when overridden). An out-of-range override growth is clamped via clampGrowth before use.
  </behavior>
  <action>
    Create lib/valuation/comps.test.ts co-located per CONVENTIONS. Import the
    not-yet-existing exports from "@/lib/valuation/comps": buildCompsTable,
    clampGrowth, COMPS_YEARS, and the types CompsInputs/CompsOverrides/CompsRow/
    ScenarioKey. Write one `describe` per concern (COMPS_YEARS, math, scenario
    mapping, null propagation, clampGrowth, overrides) covering every bullet in
    <behavior>. Use exact-equality `toBe` for integer-exact products and
    `toBeCloseTo(expected, 0)` for compounded values. Assert nulls with
    `toBeNull()`. Build inputs as a small local factory that returns a valid
    CompsInputs (base_revenue value/source, peer_multiple percentiles, growth
    bear/base/bull) so each test tweaks one field. Do NOT import React or
    Supabase. Run the suite and confirm it FAILS (module missing) before Task 2.
  </action>
  <verify>
    <automated>npx vitest run lib/valuation/comps.test.ts 2>&1 | grep -qiE "cannot find module|failed|no test files? found" && echo RED-OK</automated>
  </verify>
  <done>comps.test.ts exists with the full <behavior> matrix; the suite fails only because lib/valuation/comps.ts does not yet exist (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the pure comps table builder to green</name>
  <files>lib/valuation/comps.ts</files>
  <read_first>
    - lib/agents/deep-dive-types.ts — AnalysisValuation input contract (do not edit).
    - lib/valuation/comps.test.ts — the contract just written.
  </read_first>
  <action>
    Create lib/valuation/comps.ts (no "use client", no "server-only" — a pure
    isomorphic module usable from the RSC page and the client tab). Export:
    (1) `const COMPS_YEARS = [2026, 2027, 2028, 2029, 2030] as const` and a
    `const BASE_YEAR = 2026`.
    (2) `type ScenarioKey = "bear" | "base" | "bull"` and
    `type MultiplePercentile = "p25" | "median" | "p75"`.
    (3) `interface CompsInputs` = the subset of AnalysisValuation the math needs
    (base_revenue, peer_multiple, growth); accept the full AnalysisValuation by
    typing CompsInputs = Pick<AnalysisValuation, "base_revenue" | "peer_multiple"
    | "growth"> so callers pass the stored object directly.
    (4) `interface CompsOverrides { growth?: number | null; multiplePercentile?:
    MultiplePercentile }` — when `growth` is set it replaces ALL three scenario
    growth rates (single-lever user override, VAL-04); when `multiplePercentile`
    is set it replaces the per-scenario percentile mapping with that one
    percentile for all columns.
    (5) `interface CompsRow { year: number; bear: number | null; base: number |
    null; bull: number | null }`.
    (6) `function clampGrowth(g: number | null | undefined): number | null` —
    return null for non-finite/absent input (never fabricate), else clamp to
    [GROWTH_MIN, GROWTH_MAX] with GROWTH_MIN = -0.5 and GROWTH_MAX = 3.0 exported
    as consts for the tab's input bounds.
    (7) `function buildCompsTable(inputs: CompsInputs, overrides?: CompsOverrides):
    CompsRow[]` — for each year in COMPS_YEARS with n = year − BASE_YEAR, compute
    each scenario cell via a private `impliedValuation(baseRevenue, growth,
    multiple, n)` = baseRevenue == null || multiple == null ? null : baseRevenue ×
    (1 + growth) ** n × multiple. Default scenario→growth = {bear: growth.bear,
    base: growth.base, bull: growth.bull}; default scenario→multiple = {bear:
    peer_multiple.p25, base: peer_multiple.median, bull: peer_multiple.p75}. When
    overrides.growth is provided, run it through clampGrowth and use the clamped
    value for all three columns. When overrides.multiplePercentile is provided,
    use the single chosen peer_multiple[percentile] for all three columns. Return
    exactly 5 rows. Keep it allocation-light and side-effect-free.
    Match the surrounding file style (2-space indent, double quotes, trailing
    commas, JSDoc on every export explaining the null-honesty and override rules).
  </action>
  <verify>
    <automated>npx vitest run lib/valuation/comps.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>All comps.test.ts cases pass (GREEN); tsc --noEmit is clean; the module exports buildCompsTable/clampGrowth/COMPS_YEARS/GROWTH_MIN/GROWTH_MAX + the CompsInputs/CompsOverrides/CompsRow/ScenarioKey/MultiplePercentile types; AnalysisValuation was not modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored valuation JSONB → comps math | `AnalysisValuation` read from DB may carry nulls; must never be coerced to a fabricated 0. |
| user override input → comps math | Client-supplied growth %/percentile crosses into the calculation (handled in Wave 2 UI, clamped here via clampGrowth). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | buildCompsTable null handling | mitigate | null base_revenue/multiple → null cell, unit-tested; never emit 0 as a fabricated figure |
| T-05-02 | Tampering | user growth override | mitigate | clampGrowth bounds override to [-0.5, 3.0]; non-finite → null; unit-tested |
| T-05-03 | Information disclosure | pure module | accept | no I/O, no secrets, no PII in this module |
</threat_model>

<verification>
- `npx vitest run lib/valuation/comps.test.ts` — all cases green.
- `npx tsc --noEmit` — clean.
- `npx eslint lib/valuation/comps.ts lib/valuation/comps.test.ts` — clean.
- `git diff --exit-code lib/agents/deep-dive-types.ts` — AnalysisValuation UNCHANGED.
</verification>

<success_criteria>
- buildCompsTable returns 5 rows (2026–2030) with correct `revenue × (1+growth)^n
  × multiple` products and Bear/Base/Bull mapped to p25/median/p75.
- Null base_revenue or null multiple yields null cells, never 0.
- clampGrowth bounds overrides to [-0.5, 3.0] and returns null for non-finite.
- No React/Supabase import; AnalysisValuation and comps helpers untouched.
</success_criteria>

<output>
Create `.planning/phases/05-valuation-targets-tab/05-01-SUMMARY.md` when done.
</output>
