---
phase: 05-valuation-targets-tab
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - components/company/valuation-targets.tsx
  - components/company/historical-financials.tsx
  - app/(app)/companies/[id]/page.tsx
autonomous: false
requirements: [VAL-01, VAL-02, VAL-03, VAL-04, VAL-05]
tags: [valuation, ui, tab]

must_haves:
  truths:
    - "A new 'Valuation Targets' tab sits between Valuation and Funding Rounds"
    - "The tab renders a 2026–2030 × Bear/Base/Bull implied-valuation table computed via buildCompsTable"
    - "Editing growth % or multiple percentile recomputes the table AND chart live, client-side"
    - "Every table cell exposes its inputs (revenue × multiple = value) on hover"
    - "The inputs panel shows base_revenue+source, current_valuation, peer percentiles with n_peers/n_sec_verified + SEC badge, and the growth proposal + ConfidenceChip + rationale"
    - "An always-visible 'implied by peer comparables … not a forecast' disclaimer is shown"
    - "No analysis → DeepDiveEmpty; analysis present but null base_revenue/multiple → an honest 'insufficient comps inputs' panel naming what is missing"
    - "The Valuation tab shows a historical_financials block (chips per field) with existing timeline/table preserved"
  artifacts:
    - path: "components/company/valuation-targets.tsx"
      provides: "The client Valuation Targets tab (inputs panel, live table + recharts chart, disclaimer, empty/insufficient states)"
      min_lines: 120
    - path: "components/company/historical-financials.tsx"
      provides: "Server-safe VAL-01 render block for the Valuation tab (chip-labelled gross_margin/burn_rate/runway/acv + empty state)"
      min_lines: 30
    - path: "app/(app)/companies/[id]/page.tsx"
      provides: "New tab trigger + TabsContent wired with serializable AnalysisValuation; VAL-01 block on the Valuation tab"
      contains: "Valuation Targets"
  key_links:
    - from: "components/company/valuation-targets.tsx"
      to: "lib/valuation/comps.ts (buildCompsTable, clampGrowth, COMPS_YEARS)"
      via: "useMemo recompute on growth/percentile state"
      pattern: "buildCompsTable"
    - from: "app/(app)/companies/[id]/page.tsx"
      to: "components/company/valuation-targets.tsx"
      via: "TabsContent passes analysis.valuation (serializable AnalysisValuation) only"
      pattern: "ValuationTargets"
    - from: "app/(app)/companies/[id]/page.tsx"
      to: "components/company/historical-financials.tsx"
      via: "Valuation tab renders sections.historical_financials"
      pattern: "HistoricalFinancials"
---

<objective>
Ship the user-facing Phase 5 UI: (1) a NEW interactive "Valuation Targets" tab
between Valuation and Funding Rounds — a client component that owns the growth %
input and multiple-percentile selector and recomputes the 2026–2030 Bear/Base/Bull
table + recharts chart live client-side (VAL-02, VAL-03, VAL-04, VAL-05); and
(2) a factual `historical_financials` block on the existing Valuation tab (VAL-01),
leaving the timeline/table untouched. All $ figures come exclusively from
`buildCompsTable` (Plan 01). Ends with a human-verify checkpoint: the orchestrator
regenerates a demo company and eyeballs the live tab in the browser.

Purpose: Turn the stored comps inputs + math into the transparent, honestly-
disclaimed valuation explorer that is this milestone's final deliverable.
Output: two components + the wired page + a verified live render.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-valuation-targets-tab/05-CONTEXT.md

<read_first>
- lib/valuation/comps.ts — buildCompsTable, clampGrowth, COMPS_YEARS, GROWTH_MIN,
  GROWTH_MAX, CompsInputs, CompsOverrides, CompsRow, MultiplePercentile (Plan 01;
  the ONLY source of $ figures — do not re-derive math in the component).
- lib/agents/deep-dive-types.ts — AnalysisValuation (the serializable prop the
  server page passes to the tab) + HistoricalFinancialsSection / OverviewSections
  (Plan 02; the VAL-01 block reads sections.historical_financials).
- app/(app)/companies/[id]/page.tsx — the RSC page: the `Tabs`/`TabsList`/
  `TabsTrigger` block (~325–335, add the trigger between "valuation" and
  "funding"), the `<TabsContent value="valuation">` block (~459–516, add the
  VAL-01 block), and how `analysis` (CompanyAnalysisRow | null) + `analysisStale`
  are already computed. The page passes ONLY serializable data across the RSC→
  client boundary (no lucide components as props).
- components/company/valuation-timeline.tsx — the recharts pattern to MATCH:
  "use client", ResponsiveContainer/LineChart/XAxis/YAxis/CartesianGrid/Tooltip/
  Line, hsl(var(--chart-N)) colors, hsl(var(--popover)) tooltip, formatCurrency
  tickFormatter, the fixed 260px height + legend row.
- components/dashboard/portfolio-charts.tsx — additional recharts styling
  (axis/tooltip consts) and multi-line usage.
- components/company/confidence-chip.tsx — ConfidenceChip (basis/confidence),
  DeepDiveEmpty (message + action) — server-safe, reuse verbatim.
- components/company/competitors-analysis.tsx — the SEC-verified badge pattern
  (ShieldCheck + "SEC", `title="A matching SEC Form D filing was found"`) to
  reuse for the peer-multiple provenance; also the overflow-x table wrapper style.
- lib/utils.ts — formatCurrency / formatMultiple / formatPercent (tabular-nums
  formatting; note formatCurrency is compact by default). Use these; add no new
  number formatting.
- components/ui/tabs.tsx, components/ui/card.tsx, components/ui/badge.tsx,
  components/ui/table.tsx — existing primitives to compose with.

<interfaces>
From lib/valuation/comps.ts (Plan 01):
  const COMPS_YEARS: readonly [2026,2027,2028,2029,2030];
  const GROWTH_MIN = -0.5; const GROWTH_MAX = 3.0;
  type MultiplePercentile = "p25" | "median" | "p75";
  type CompsRow = { year: number; bear: number|null; base: number|null; bull: number|null };
  function buildCompsTable(inputs: CompsInputs, overrides?: CompsOverrides): CompsRow[];
  function clampGrowth(g: number|null|undefined): number|null;

From lib/agents/deep-dive-types.ts:
  interface AnalysisValuation { base_revenue:{value:number|null;source:string|null};
    current_valuation:number|null;
    peer_multiple:{median:number|null;p25:number|null;p75:number|null;n_peers:number;n_sec_verified:number};
    growth:{base:number;bear:number;bull:number;confidence:"low"|"med"|"high";rationale:string}; }
  interface HistoricalFinancialsSection { gross_margin?; burn_rate?; runway?; acv?: LabelledField }

From components/company/confidence-chip.tsx:
  function ConfidenceChip({ basis, confidence, className? }): JSX;
  function DeepDiveEmpty({ message?, action?, className? }): JSX;
</interfaces>
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the historical_financials render block (VAL-01)</name>
  <files>components/company/historical-financials.tsx</files>
  <read_first>
    - components/company/competitors-analysis.tsx — server-component pattern +
      label-eyebrow + ConfidenceChip usage (no "use client", no lucide props).
    - components/company/overview-sections.tsx (lines 62–80) — the `Field` helper
      (labelled narrative + ConfidenceChip) to mirror.
    - lib/agents/deep-dive-types.ts — HistoricalFinancialsSection.
  </read_first>
  <action>
    Create components/company/historical-financials.tsx as a SERVER Component (no
    "use client" — it renders inside the RSC page and only uses ConfidenceChip +
    plain markup). Export `HistoricalFinancials({ financials }: { financials:
    HistoricalFinancialsSection | undefined })`. Define an ordered field list
    [{key:"gross_margin",label:"Gross margin"},{key:"burn_rate",label:"Burn rate"},
    {key:"runway",label:"Runway"},{key:"acv",label:"ACV"}]. Render each present
    LabelledField as a compact chip-labelled block: label-eyebrow caption, the
    `text` as a `<p className="text-sm">` (React auto-escaped — NO
    dangerouslySetInnerHTML), a ConfidenceChip with the field's basis/confidence,
    and the `source` (if any) as muted caption. Lay the fields out as a responsive
    grid (e.g. sm:grid-cols-2) inside a Card, headed by a "Financial detail"
    label. If `financials` is undefined OR every field is absent, render nothing
    (the page shows the DeepDiveEmpty CTA — see Task 3). Match the flat/hairline
    design system (tabular-nums where numeric text appears). Keep it purely
    presentational and serializable-prop-only.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint components/company/historical-financials.tsx</automated>
  </verify>
  <done>HistoricalFinancials renders gross_margin/burn_rate/runway/acv as chip-labelled blocks with ConfidenceChip + source; absent fields omitted; returns null when nothing present; server component, no dangerouslySetInnerHTML; tsc/eslint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build the interactive Valuation Targets tab (VAL-02/03/04/05)</name>
  <files>components/company/valuation-targets.tsx</files>
  <read_first>
    - lib/valuation/comps.ts — buildCompsTable / clampGrowth / COMPS_YEARS /
      GROWTH_MIN / GROWTH_MAX / MultiplePercentile / CompsRow.
    - components/company/valuation-timeline.tsx — recharts styling to match.
    - components/company/competitors-analysis.tsx — SEC badge (ShieldCheck+"SEC").
    - components/company/confidence-chip.tsx — ConfidenceChip / DeepDiveEmpty.
    - lib/utils.ts — formatCurrency / formatMultiple / formatPercent.
  </read_first>
  <action>
    Create components/company/valuation-targets.tsx with "use client". Export
    `ValuationTargets({ valuation, deepDiveAction }: { valuation: AnalysisValuation
    | null; deepDiveAction?: React.ReactNode })`. Behavior:
    (A) EMPTY STATE — if `valuation` is null, render `DeepDiveEmpty` (message
    "Run deep-dive to model valuation targets.", action = deepDiveAction).
    (B) INSUFFICIENT STATE — if valuation.base_revenue.value == null OR all three
    peer_multiple percentiles (p25/median/p75) are null, render an honest
    "insufficient comps inputs" panel (dashed-border card) that NAMES exactly
    what is missing (e.g. "No base revenue on record" and/or "No SEC-verified peer
    multiples yet"), plus the disclaimer. Do NOT fabricate a table.
    (C) INTERACTIVE STATE — otherwise:
      - State: `growthOverride` (number, default = valuation.growth.base) and
        `percentile` (MultiplePercentile, default "median"). Growth input is a
        controlled number field in PERCENT (store fraction; display via
        formatPercent), bounded to [GROWTH_MIN*100, GROWTH_MAX*100] and passed
        through clampGrowth before compute. The percentile selector is a
        p25/median/p75 control (native <select> or button group).
      - `const rows = useMemo(() => buildCompsTable(valuation, { growth:
        growthOverride, multiplePercentile: percentile }), [valuation,
        growthOverride, percentile])` — the LIVE recompute (VAL-04). Do NOT
        duplicate the math; call buildCompsTable.
      - PRESET display: also show the un-overridden Bear/Base/Bull presets
        (buildCompsTable(valuation) with no overrides) so the agent presets stay
        visible alongside the user-adjusted scenario (per CONTEXT discretion:
        render presets as the table columns and reflect the override into them, or
        add the adjusted column — choose the table-columns approach: Bear/Base/Bull
        columns recompute from the current growth + percentile).
      - TABLE: rows = COMPS_YEARS, columns = Bear/Base/Bull. Each cell shows
        formatCurrency(value) or "—" when null. Each cell carries a `title`
        tooltip exposing its inputs: e.g. `${formatCurrency(baseRevenue)} × (1+g)^n
        × ${formatMultiple(multiple)} = ${formatCurrency(value)}` (VAL-05). Use
        tabular-nums; null cells render "—", never 0.
      - CHART: a recharts LineChart of the three scenario paths across
        COMPS_YEARS, matching valuation-timeline styling (ResponsiveContainer,
        260px height, hsl(var(--chart-1/2/3)) for bear/base/bull, hsl(var(--border))
        grid, hsl(var(--popover)) tooltip, formatCurrency Y tickFormatter, a small
        legend row). Skip null points gracefully (recharts renders gaps).
      - INPUTS PANEL (VAL-05 provenance): a card listing base revenue +
        `base_revenue.source`, current_valuation, and the peer multiple percentiles
        (p25/median/p75 via formatMultiple) with `n_peers` and `n_sec_verified`
        and the SEC badge (ShieldCheck + "SEC", same title as Competitors tab) when
        n_sec_verified > 0; plus the agent growth proposal (base/bear/bull rates via
        formatPercent) with a ConfidenceChip(basis="estimate", confidence=
        valuation.growth.confidence) and the `growth.rationale` text.
      - DISCLAIMER: an ALWAYS-VISIBLE line (muted caption, shown in interactive AND
        insufficient states): "Implied by peer comparables under the shown
        assumptions — not a forecast."
    Render all Grok-originated text (source, rationale) as JSX children only — no
    dangerouslySetInnerHTML. Keep the growth/percentile controls accessible
    (labels/aria). Match the flat/hairline design system.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint components/company/valuation-targets.tsx && grep -q "buildCompsTable" components/company/valuation-targets.tsx</automated>
  </verify>
  <done>ValuationTargets is a client component that: shows DeepDiveEmpty (null valuation) / insufficient panel (null base_revenue or all-null multiples) / interactive table+chart+inputs+disclaimer otherwise; recomputes rows via buildCompsTable in useMemo on growth/percentile change; cells carry input tooltips; peer multiples carry the SEC badge + n_peers/n_sec_verified; growth proposal shows ConfidenceChip + rationale; the disclaimer is always visible; no dangerouslySetInnerHTML; tsc/eslint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the tab + VAL-01 block into the company page</name>
  <files>app/(app)/companies/[id]/page.tsx</files>
  <read_first>
    - app/(app)/companies/[id]/page.tsx — TabsList (~325–335), Valuation
      TabsContent (~459–516), the `analysis` var + DeepDiveButton/DeepDiveEmpty
      usage already in the file.
    - components/company/valuation-targets.tsx (Task 2) + historical-financials.tsx (Task 1).
  </read_first>
  <action>
    Three additive edits in the RSC page:
    (1) Add a `<TabsTrigger value="valuation-targets">Valuation Targets</TabsTrigger>`
    in the TabsList BETWEEN the "valuation" and "funding" triggers.
    (2) Add a `<TabsContent value="valuation-targets">` that renders
    `<ValuationTargets valuation={analysis?.valuation ?? null} deepDiveAction={
    <DeepDiveButton companyId={company.id} />} />`. Pass ONLY the serializable
    `analysis.valuation` (AnalysisValuation) — do NOT pass lucide components or
    the whole analysis row; DeepDiveButton is already a client component so it is
    fine as a ReactNode child. Cast `analysis?.valuation` to AnalysisValuation |
    null (the stored column type) as the existing competitors wiring casts
    `analysis?.sections as OverviewSections`.
    (3) On the EXISTING `<TabsContent value="valuation">`, add the VAL-01 block
    ABOVE the timeline Card: if `analysis` exists render `<HistoricalFinancials
    financials={(analysis.sections as OverviewSections | undefined)?.
    historical_financials} />`; else render `<DeepDiveEmpty action={<DeepDiveButton
    companyId={company.id} />} />` (mirroring the Overview/Competitors gate). Leave
    the existing timeline Card + valuations table completely unchanged.
    Add the two imports (ValuationTargets, HistoricalFinancials); AnalysisValuation
    is available from the existing "@/lib/agents/deep-dive-types" import — extend
    that import line. Change nothing else on the page.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint "app/(app)/companies/[id]/page.tsx" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>The "Valuation Targets" tab trigger sits between Valuation and Funding Rounds; its TabsContent renders ValuationTargets with only analysis.valuation; the Valuation tab shows the HistoricalFinancials block (or DeepDiveEmpty) above the preserved timeline/table; only serializable data crosses the RSC boundary; next build succeeds; tsc/eslint clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the live Valuation Targets tab + VAL-01 block</name>
  <what-built>
    A new interactive "Valuation Targets" tab (2026–2030 Bear/Base/Bull comps
    table + live recharts chart + inputs panel + always-on "not a forecast"
    disclaimer, driven by a growth % input and a p25/median/p75 selector) plus a
    factual historical_financials block on the Valuation tab. All $ figures come
    from the pure, tested comps math (Plan 01).
  </what-built>
  <how-to-verify>
    The orchestrator runs this — DO NOT ask the developer to run raw commands
    themselves beyond the two below.
    1. Regenerate a demo company's analysis so both new sections are populated:
       `node --conditions=react-server --import tsx scripts/regen-deep-dive.ts "Replit"`
       Confirm the script's output includes a non-"(none)" `historical_financials:`
       line (from Plan 02).
    2. Start the app (`npm run dev`) and open the Replit company page.
    3. Click the "Valuation Targets" tab — confirm it appears BETWEEN Valuation
       and Funding Rounds. Verify against Replit's real stored row (base_revenue
       $240M, peer_multiple 13.5/14.5/15.5 with n_peers=14 / n_sec_verified=2,
       growth 0.25/0.45/0.70):
         - The Base 2026 cell reads ~$3.48B (240M × 14.5); Bull 2026 ~$3.72B;
           Bear 2026 ~$3.24B. Later years compound upward.
         - Hovering a cell shows its inputs (revenue × multiple = value).
         - Changing the growth % input and the p25/median/p75 selector updates the
           table AND the chart immediately (no page reload).
         - The inputs panel shows base revenue + source, current valuation, the
           three peer percentiles with "14 peers · 2 SEC-verified" and the SEC
           badge, and the growth proposal with its confidence chip + rationale.
         - The "Implied by peer comparables under the shown assumptions — not a
           forecast." disclaimer is visible.
    4. Open the Valuation tab — confirm the historical_financials chip block
       (gross margin / burn / runway / ACV as available, each with a Fact/Estimate
       + confidence chip) renders ABOVE the existing valuation timeline + table,
       and that the timeline/table are unchanged.
    5. Sanity: no console errors, no hydration warnings, numbers use tabular-nums.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looked wrong (wrong tab order, stale cells, missing SEC badge, missing disclaimer, broken VAL-01 block, hydration error).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC page → client tab | Only serializable AnalysisValuation may cross; no lucide components as props. |
| stored analysis text → DOM | Grok-originated source/rationale/financials text rendered into the page. |
| user override input → comps math | Growth %/percentile controls feed buildCompsTable (clamped in Plan 01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | XSS via stored text | mitigate | all Grok text rendered as JSX children (React auto-escape); no dangerouslySetInnerHTML anywhere in the two components |
| T-05-08 | Information disclosure | RSC→client boundary | mitigate | page passes only analysis.valuation (serializable); DeepDiveButton is an existing client component passed as ReactNode |
| T-05-09 | Tampering | fabricated $ figures | mitigate | every $ in the tab comes from buildCompsTable; null cells render "—"; no LLM-asserted valuation number rendered |
| T-05-10 | Denial of service | override input | accept | clampGrowth bounds recompute; buildCompsTable is O(15) — no runaway |

No npm/pip/cargo installs in this plan — package legitimacy gate N/A.
</threat_model>

<verification>
- `npx tsc --noEmit`, `npx eslint` on the three files, and `npx next build` — clean.
- Grep confirms no `dangerouslySetInnerHTML` in the two new components.
- The human-verify checkpoint confirms live behavior against Replit's stored row.
- `git diff` on page.tsx shows the Valuation tab's existing timeline Card + table
  are untouched (additive block above them only).
</verification>

<success_criteria>
- "Valuation Targets" tab between Valuation and Funding Rounds renders the
  2026–2030 Bear/Base/Bull table + chart from buildCompsTable.
- Growth % and percentile overrides recompute table + chart live client-side.
- Cells expose inputs on hover; peer multiples carry source + SEC badge; the
  "not a forecast" disclaimer is always visible.
- Empty (no analysis) and insufficient (null base_revenue/multiple) states are
  honest and name what's missing — no fabricated figures.
- The Valuation tab shows the historical_financials block with the timeline/table
  preserved.
- No $ figure appears that is not derived from the comps calculation.
</success_criteria>

<output>
Create `.planning/phases/05-valuation-targets-tab/05-03-SUMMARY.md` when done.
</output>
