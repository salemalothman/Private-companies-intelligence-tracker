---
phase: 04-external-grounding-ingestion
plan: 06
type: execute
wave: 4
depends_on: ["04-01", "04-03", "04-04", "04-05"]
files_modified:
  - lib/agents/deep-dive.ts
  - lib/agents/deep-dive.test.ts
autonomous: true
requirements: [ING-05, ING-06]
must_haves:
  truths:
    - "runDeepDive's grounding gather also reads the cached form_d_rounds, peer_financials, and recent x_posts for the company/peers"
    - "The cached facts are serialized into the Grok prompt with explicit source attribution (Form D / XBRL / X)"
    - "The no-fabrication guardrail is preserved: comps stay code-computed, growth remains an LLM rate-only proposal, and cached facts are labelled by real source"
    - "Existing runDeepDive behavior (canonical/competitor/funding grounding, one Grok call, code comps, upsert) is preserved"
  artifacts:
    - path: "lib/agents/deep-dive.ts"
      provides: "extended grounding gather reading the three caches + source-attributed serialization"
      contains: "form_d_rounds"
    - path: "lib/agents/deep-dive.test.ts"
      provides: "unit test for the pure grounding-serialization of cached facts with source tags"
      contains: "describe"
  key_links:
    - from: "lib/agents/deep-dive.ts runDeepDive"
      to: "form_d_rounds / peer_financials / x_posts"
      via: "supabase.from(...).select(...) in the gather step"
      pattern: "form_d_rounds|peer_financials|x_posts"
    - from: "lib/agents/deep-dive.ts buildPrompt/summarizeGrounding"
      to: "Grok prompt"
      via: "source-attributed grounding block"
      pattern: "Form D|XBRL|source"
---

<objective>
Extend `runDeepDive`'s grounding gather (Phase 1) to ALSO read the cached
ingested facts — company-goat Form D rounds, sec-edgar peer XBRL, recent x_posts —
and fold them into the Grok prompt with explicit source attribution, so generated
fields cite real Form D / XBRL / X sources rather than the LLM alone. The comps
math and growth-rate contract are unchanged; the no-fabrication guardrail holds.

Purpose: ING-05 closes the loop — the caches populated in Plans 03/04/05 become
real grounding the analyst layer draws on. This is the last wave.
Output: extended `lib/agents/deep-dive.ts` + a new `lib/agents/deep-dive.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-external-grounding-ingestion/04-CONTEXT.md

<read_first>
- lib/agents/deep-dive.ts — the ENTIRE file. Critical spots: Step 1 GATHER (~228-275, the Promise.all reading competitors + market_valuations), summarizeGrounding (~165-205, the compact prompt block builder), buildPrompt (~142-163, the HARD RULES guard), computePeerMultiple (code comps — MUST stay untouched), Step 4 upsert. Behavior-preserving: do not change the comps computation, the growth-rate contract, or the upsert.
- lib/types.ts — FormDRoundRow, PeerFinancialRow, XPostRow (Plan 01) for the select typing.
- lib/market-cache/parse.ts (nameKey) — the name-normalization used to match cached rows to peers (already imported in deep-dive.ts).
- lib/market-cache/parse.test.ts — vitest style for the new deep-dive.test.ts.
</read_first>

<interfaces>
Existing gather (from deep-dive.ts Step 1): reads competitors (by company_id) and market_valuations (by name_key). Extend the same Promise.all with three reads:
- form_d_rounds: `.select("*").eq("company_id", company.id)` → FormDRoundRow[] (this company + its named competitors' rounds).
- peer_financials: for the ranked peers, `.select("*").in("cik", peerCiks)` OR `.in("ticker", peerTickers)` — resolve by whatever the peers carry; peer_financials is authenticated-read reference data.
- x_posts: `.select("*").eq("company_id", company.id).order("posted_at",{ascending:false}).limit(N)` → recent XPostRow[].

Serialization: cached facts are appended to the existing summarizeGrounding block as clearly source-tagged lines, e.g. "Form D (SEC, source: company-goat): <subject> raised <offering_amount> on <filing_date>", "Peer XBRL (SEC): <entity> revenue <revenue> (<fiscal_period>)", "X post (source: x-twitter): <text> (<posted_at>)". The buildPrompt HARD RULES already forbid fabrication — add a sentence instructing the model to attribute any fact it uses to its provided source tag and to treat un-tagged claims as estimates.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure source-attributed serialization of cached facts with unit test</name>
  <files>lib/agents/deep-dive.ts, lib/agents/deep-dive.test.ts</files>
  <read_first>lib/agents/deep-dive.ts (summarizeGrounding ~165-205), lib/types.ts (FormDRoundRow/PeerFinancialRow/XPostRow), lib/market-cache/parse.test.ts</read_first>
  <behavior>
    - summarizeCachedGrounding({ formD, peerFin, posts }): given arrays of the three cache rows, returns a compact block where each line is prefixed with its real source tag ("Form D (company-goat)", "Peer XBRL (sec-edgar)", "X post (x-twitter)").
    - Numeric/absent fields render as their real value or "?" — no fabricated numbers.
    - Empty arrays → the section is omitted entirely (no empty headers, no invented placeholder facts).
    - The function is pure (no Supabase, no network) so it is unit-testable.
    - Peer XBRL lines include fiscal_period so revenue is never period-ambiguous.
  </behavior>
  <action>
Add a pure exported helper summarizeCachedGrounding(cached: { formD: FormDRoundRow[]; peerFin: PeerFinancialRow[]; posts: XPostRow[] }): string to lib/agents/deep-dive.ts, mirroring the style of the existing summarizeGrounding (compact, `filter(Boolean).join`). Every line carries its source tag; empty sets omit their section. Do NOT alter summarizeGrounding, computePeerMultiple, deriveBaseRevenue, or the growth contract.

Create lib/agents/deep-dive.test.ts with a `describe` covering summarizeCachedGrounding: populated all-three, mixed-empty, all-empty, and a missing-numeric case. (The file currently has no test; export the helper so vitest can import it — the pure helpers already avoid runtime Supabase.)
  </action>
  <verify>
    <automated>npx vitest run lib/agents/deep-dive.test.ts</automated>
  </verify>
  <done>Tests pass; every cached fact line is source-tagged; empty sets omit their section; no fabricated numbers; peer XBRL lines carry fiscal_period; existing pure helpers are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the three cache reads into runDeepDive's gather + prompt</name>
  <files>lib/agents/deep-dive.ts</files>
  <read_first>lib/agents/deep-dive.ts (Step 1 GATHER ~228-275, buildPrompt ~142-163), lib/types.ts (the three Row types)</read_first>
  <action>
In runDeepDive Step 1, extend the existing Promise.all to also read: form_d_rounds by company_id; x_posts by company_id ordered by posted_at desc limited to a sensible N (e.g. 10); and peer_financials for the ranked peers (build the peer identifier list from the ranking/competitor rows — match by ticker or cik where available; if peers carry neither, skip the peer_financials read gracefully with an empty array). Type each result via the Row types from lib/types.ts. Treat null/error data as empty arrays (never throw — matches the existing degrade-not-throw convention).

Fold summarizeCachedGrounding(...) output into the prompt: append it to the grounding string passed to buildPrompt (or concatenate after summarizeGrounding's block). Add one sentence to buildPrompt's guard: instruct the model to attribute any fact it uses to the provided source tag (Form D / XBRL / X) and to label anything not backed by a tagged fact as an "estimate" — reinforcing, not replacing, the existing HARD RULES.

Behavior-preserving invariants (MUST hold): computePeerMultiple / deriveBaseRevenue still compute comps in code; growth stays an LLM rate-only proposal; the Step 4 upsert on company_id is unchanged; the function still degrades to an empty-but-timestamped analysis rather than throwing.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint lib/agents/deep-dive.ts && npx vitest run</automated>
  </verify>
  <done>tsc + eslint clean and the full vitest suite is green; runDeepDive reads all three caches and passes source-attributed cached grounding into the Grok prompt; comps remain code-computed and the growth contract + upsert are unchanged; missing cache data degrades to empty (no throw).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached rows → Grok prompt | cache content (already source-tagged) is serialized into an LLM prompt |
| RLS user client → form_d_rounds / x_posts | runDeepDive runs under the RLS user client (button) — owner-scoped reads enforced by RLS |
| model output → stored analysis | the no-fabrication guard must survive the added grounding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Spoofing/fabrication | LLM citing facts | mitigate | buildPrompt guard extended to require source attribution + label untagged claims as estimates; comps stay code-computed (LLM emits only growth rates) — the no-fabrication guardrail is preserved |
| T-04-20 | Information Disclosure | cache reads under RLS user client | mitigate | form_d_rounds/x_posts are owner-scoped (all_own RLS); runDeepDive reads under the RLS user session, so a user only ever grounds on their own cached rows; peer_financials is public reference data |
| T-04-21 | Tampering | cached-fact serialization | mitigate | summarizeCachedGrounding renders real values or "?", never fabricated numbers; unit-tested |
| T-04-SC | Tampering | package installs | mitigate | no new packages; slopcheck N/A |
</threat_model>

<verification>
- `npx vitest run` (full suite) green, including deep-dive.test.ts.
- `npx tsc --noEmit` + `npx eslint lib/agents/deep-dive.ts` clean.
- Code review confirms comps stay code-computed, growth remains rate-only, upsert unchanged, and cached grounding is source-attributed in the prompt.
</verification>

<success_criteria>
runDeepDive reads the cached Form D / peer XBRL / X facts as additional grounding
and cites them by real source, with the no-fabrication guardrail and code-computed
comps preserved (ING-05, ING-06).
</success_criteria>

<output>
Create `.planning/phases/04-external-grounding-ingestion/04-06-SUMMARY.md` when done.
</output>
