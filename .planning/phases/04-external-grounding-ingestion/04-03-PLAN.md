---
phase: 04-external-grounding-ingestion
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - lib/ingest/company-goat.ts
  - lib/ingest/company-goat.test.ts
autonomous: true
requirements: [ING-03, ING-06]
must_haves:
  truths:
    - "For each portfolio company + competitor with a domain, company-goat Form D rounds are fetched via funding --domain and upserted into form_d_rounds"
    - "When funding returns is_ambiguous/cik_summaries the module re-queries funding --cik <id> and only then trusts amounts (no name-fragment amounts cached)"
    - "Each row is source-tagged (source='company-goat', fetched_at, cik/subject) and scoped to the correct company_id/user_id; re-runs upsert on the natural key"
    - "A missing company-goat binary or absent domain skips that target cleanly"
  artifacts:
    - path: "lib/ingest/company-goat.ts"
      provides: "ingestCompanyGoat SourceModule + pure mapEnvelopeToRounds mapper"
      exports: ["ingestCompanyGoat", "mapFundingResult"]
    - path: "lib/ingest/company-goat.test.ts"
      provides: "unit tests for the pure mapper across normal / ambiguous / empty results"
      contains: "describe"
  key_links:
    - from: "lib/ingest/company-goat.ts"
      to: "form_d_rounds table"
      via: "admin.from('form_d_rounds').upsert(..., { onConflict: 'company_id,subject,accession' })"
      pattern: "form_d_rounds"
    - from: "lib/ingest/company-goat.ts"
      to: "lib/ingest/cli.ts"
      via: "runAgentCli + resolveCik"
      pattern: "runAgentCli|resolveCik"
---

<objective>
Implement the company-goat source module: for every enumerated target with a
domain, fetch SEC Form D rounds + startup signals, apply mandatory CIK
disambiguation, and upsert source-tagged rows into `form_d_rounds`. This is the
private-target/competitor grounding (Form D offering amounts, filing dates,
signals) that runDeepDive (Plan 06) will cite.

Purpose: ING-03 — no ambiguous name-fragment amounts (the Replit failure) may
enter the cache; every amount must be tied to a disambiguated CIK.
Output: `lib/ingest/company-goat.ts` + `lib/ingest/company-goat.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-external-grounding-ingestion/04-CONTEXT.md
@.planning/codebase/INTEGRATIONS.md

<read_first>
- lib/ingest/cli.ts (Plan 02) — runAgentCli, parseEnvelope, resolveCik, hasBinary, requireEnv, AGENT_FLAGS.
- lib/ingest/types.ts (Plan 02) — IngestTarget, SourceSummary, CikResolution.
- lib/agents/sentiment.ts — the SourceModule return-summary + idempotent-upsert convention.
- lib/types.ts — FormDRoundRow / FormDRoundInsert (Plan 01) for the exact insert shape.
- lib/connectors/sec-edgar.ts (searchFormD / SPV-filter at ~30-55) — the existing name-normalization + SPV/fund exclusion heuristic to reuse for filtering false-positive filers.
</read_first>

<interfaces>
CLI commands (exact):
- `company-goat-pp-cli funding --domain <domain>` + AGENT_FLAGS → `{meta, results}` with Form D rounds; may carry `is_ambiguous` / `cik_summaries`.
- `company-goat-pp-cli funding --cik <cik>` + AGENT_FLAGS → disambiguated rounds for one entity.
- `company-goat-pp-cli snapshot --domain <domain>` + AGENT_FLAGS → signals (github/HN/legitimacy).
- Free source; optional COMPANY_PP_CONTACT_EMAIL / GITHUB_TOKEN via env (raise rate limits).

FormDRoundInsert shape (Plan 01): { company_id, user_id?, subject, cik?, accession, offering_amount?, amount_sold?, filing_date?, exemption?, related_persons?, signals?, source?, source_url?, fetched_at? }. Natural key: (company_id, subject, accession).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure funding-result mapper + disambiguation branch with unit tests</name>
  <files>lib/ingest/company-goat.ts, lib/ingest/company-goat.test.ts</files>
  <read_first>lib/ingest/cli.ts (resolveCik), lib/types.ts (FormDRoundInsert), lib/connectors/sec-edgar.ts (SPV filter ~48-55)</read_first>
  <behavior>
    - mapFundingResult(results, target): normal rounds array → array of FormDRoundInsert with offering_amount/amount_sold/filing_date/exemption/accession/cik mapped, subject=target.subject, company_id/user_id from target, source='company-goat', fetched_at set.
    - Each round missing an `accession` is dropped (accession is the natural-key anchor — cannot upsert without it, never fabricate one).
    - SPV/fund-vehicle filers (name matches /\bspv\b|\bfund\b|a series of/i) are excluded (reuse the sec-edgar heuristic) — these are not the company's own raise.
    - Numeric fields absent → null, never zero (no fabricated amounts).
    - When resolveCik(result) reports ambiguous, mapFundingResult returns [] and signals the caller to re-query (a dedicated needsCikRequery(result) helper returns the candidate CIKs).
    - Empty/absent results → [].
  </behavior>
  <action>
Implement pure helpers in `lib/ingest/company-goat.ts`: mapFundingResult(results: unknown, target: IngestTarget): FormDRoundInsert[] and needsCikRequery(result: unknown): string[] (candidate CIKs when ambiguous, else []). Reuse the SPV/fund exclusion regex from lib/connectors/sec-edgar.ts. Do not import `server-only` into the pure helpers (keep them vitest-runnable); if the module needs `server-only` for the impure part, put helpers in the same file but ensure tests import only the pure exports — OR keep helpers export-only and confirm vitest can load the file. (Prefer: no `server-only` at module top; guard impure calls behind the exported async fn.)

Write `lib/ingest/company-goat.test.ts` covering every <behavior> case with inline fixtures modeled on the real envelope shape (normal rounds, ambiguous with cik_summaries, SPV filer, missing accession, empty).
  </action>
  <verify>
    <automated>npx vitest run lib/ingest/company-goat.test.ts</automated>
  </verify>
  <done>All mapper tests pass; ambiguous results yield [] + candidate CIKs; SPV filers and accession-less rounds are dropped; no numeric field is defaulted to zero.</done>
</task>

<task type="auto">
  <name>Task 2: ingestCompanyGoat SourceModule — fetch, disambiguate, upsert</name>
  <files>lib/ingest/company-goat.ts</files>
  <read_first>lib/ingest/cli.ts, lib/agents/sentiment.ts (upsert loop + summary), lib/types.ts (FormDRoundInsert)</read_first>
  <action>
Implement `ingestCompanyGoat(admin, targets): Promise<SourceSummary>`. Preflight `hasBinary("company-goat-pp-cli")`; if absent return { source:"company-goat", upserted:0, skipped:targets.length, status:"skipped", detail:"binary not on PATH" }. Read COMPANY_PP_CONTACT_EMAIL / GITHUB_TOKEN via requireEnv and pass through the env to runAgentCli (never log them).

For each target with a domain (skip + count targets with no domain): call runAgentCli("company-goat-pp-cli", ["funding","--domain",target.domain]). On {ok:false} → skip that target (continue). Run needsCikRequery on the result; if it returns candidate CIKs, pick the correct entity — when exactly one candidate, re-call funding --cik; when multiple, prefer the candidate whose name best matches target.subject via the existing name-normalization from sec-edgar.ts, else skip (do NOT cache an ambiguous amount). Optionally also call `snapshot --domain` and merge signals into the rows' `signals` jsonb.

Map via mapFundingResult, then `admin.from("form_d_rounds").upsert(rows, { onConflict: "company_id,subject,accession" })`. Set user_id explicitly from target.userId (service-role bypasses RLS — writes MUST be scoped to the owner). Accumulate upserted/skipped counts and return the summary; status "partial" if any per-target error occurred, else "success". Never throw out of the loop.

Add `import "server-only"` only if it does not break vitest loading of the pure helpers (Task 1 keeps helpers loadable); prefer guarding server-only usage so the test file still runs.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint lib/ingest/company-goat.ts && npx vitest run lib/ingest/company-goat.test.ts</automated>
  </verify>
  <done>tsc + eslint + vitest clean; the module skips cleanly with no binary/no domain; ambiguous funding triggers a --cik re-query or a skip (never a name-fragment amount); rows upsert on (company_id, subject, accession) with user_id set from the target; secrets are passed via env and never logged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| company-goat stdout → cache | untrusted external data; parsed via parseEnvelope, mapped defensively |
| target.domain/subject → CLI arg | DB-derived args passed to execFile (array, no shell) |
| service-role write → form_d_rounds | RLS bypassed; user_id must be set from target.userId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Spoofing/fabrication | ambiguous Form D amount | mitigate | needsCikRequery → funding --cik re-query or skip; SPV/fund filers excluded; unit-tested — no name-fragment amount is ever cached |
| T-04-09 | Elevation of Privilege | service-role upsert | mitigate | user_id set explicitly from target.userId; company_id from the enumerated owner row; parameterized upsert (no SQL string building) |
| T-04-10 | Information Disclosure | COMPANY_PP_CONTACT_EMAIL / GITHUB_TOKEN | mitigate | read via requireEnv, passed through env to execFile, never logged |
| T-04-SC | Tampering | package installs | mitigate | no new packages; company-goat-pp-cli is a pre-installed local binary; slopcheck N/A |
</threat_model>

<verification>
- `npx vitest run lib/ingest/company-goat.test.ts` green (mapper + disambiguation).
- `npx tsc --noEmit` + `npx eslint lib/ingest/company-goat.ts` clean.
- Manual (optional, requires binary): `npm run ingest` upserts form_d_rounds rows for a target with a domain and skips those without; ambiguous entities never yield cached amounts.
</verification>

<success_criteria>
company-goat Form D rounds + signals are cached per company/competitor with
mandatory CIK disambiguation and source-tagging, idempotent on re-run (ING-03, ING-06).
</success_criteria>

<output>
Create `.planning/phases/04-external-grounding-ingestion/04-03-SUMMARY.md` when done.
</output>
