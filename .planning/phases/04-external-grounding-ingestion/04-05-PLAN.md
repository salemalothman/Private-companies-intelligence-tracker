---
phase: 04-external-grounding-ingestion
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - lib/ingest/x-twitter.ts
  - lib/ingest/x-twitter.test.ts
autonomous: true
requirements: [ING-04, ING-06]
must_haves:
  truths:
    - "The module preflights x-twitter doctor and only runs when auth_lanes.app_only_api is available (X_BEARER_TOKEN present); otherwise it skips cleanly"
    - "Company + competitor posts are synced read-only and upserted into x_posts keyed by (company_id, post_id)"
    - "Rows are source-tagged (source='x-twitter', fetched_at, subject/handle) and scoped to the correct company_id/user_id; re-runs upsert on the natural key"
    - "The module never posts/replies/quotes (read-only) and never logs the bearer token"
  artifacts:
    - path: "lib/ingest/x-twitter.ts"
      provides: "ingestXTwitter SourceModule + pure mapTweetsResult mapper"
      exports: ["ingestXTwitter", "mapTweetsResult"]
    - path: "lib/ingest/x-twitter.test.ts"
      provides: "unit tests for the pure tweets mapper + doctor-lane gate"
      contains: "describe"
  key_links:
    - from: "lib/ingest/x-twitter.ts"
      to: "x_posts table"
      via: "admin.from('x_posts').upsert(..., { onConflict: 'company_id,post_id' })"
      pattern: "x_posts"
    - from: "lib/ingest/x-twitter.ts"
      to: "lib/ingest/cli.ts"
      via: "runAgentCli doctor preflight + sync"
      pattern: "runAgentCli"
---

<objective>
Implement the x-twitter source module: preflight `doctor` for the app-only lane,
and when the bearer token is present, sync company + competitor posts read-only and
cache source-tagged rows in `x_posts` for the News tab + sentiment agent. This is
the news/sentiment grounding runDeepDive (Plan 06) will cite.

Purpose: ING-04 — opt-in on X_BEARER_TOKEN (paid per-read); read-only; env-only
secret; idempotent.
Output: `lib/ingest/x-twitter.ts` + `lib/ingest/x-twitter.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-external-grounding-ingestion/04-CONTEXT.md
@.planning/codebase/INTEGRATIONS.md

<read_first>
- lib/ingest/cli.ts (Plan 02) — runAgentCli, parseEnvelope, hasBinary, requireEnv.
- lib/ingest/types.ts (Plan 02) — IngestTarget, SourceSummary.
- lib/types.ts — XPostRow / XPostInsert (Plan 01) for the exact insert shape.
- lib/agents/sentiment.ts + lib/news/classify.ts — the existing news→sentiment wiring the cached posts should be compatible with (scoreSentiment over title/summary).
</read_first>

<interfaces>
CLI commands (exact):
- `x-twitter-pp-cli doctor` + AGENT_FLAGS → JSON with `auth_lanes.app_only_api.status` ("missing" when no X_BEARER_TOKEN, confirmed). Gate on status !== "missing".
- `x-twitter-pp-cli sync --resources tweets --since <window>` + AGENT_FLAGS → tweets for the configured handles/keywords (read-only).
- Auth: app-only bearer via X_BEARER_TOKEN env (paid per-read → opt-in on presence).
- Read-only only — NEVER post/reply/quote (no write subcommands).

XPostInsert shape (Plan 01): { company_id, user_id?, subject, handle?, post_id, text?, author?, posted_at?, url?, metrics?, source?, fetched_at? }. Natural key: (company_id, post_id).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure tweets mapper + doctor-lane gate with unit tests</name>
  <files>lib/ingest/x-twitter.ts, lib/ingest/x-twitter.test.ts</files>
  <read_first>lib/types.ts (XPostInsert), lib/ingest/cli.ts (parseEnvelope shape), lib/market-cache/parse.test.ts (vitest style)</read_first>
  <behavior>
    - appOnlyLaneReady(doctorResult): result with auth_lanes.app_only_api.status !== "missing" → true; status "missing" or absent → false.
    - mapTweetsResult(results, target): tweets array → XPostInsert[] with post_id (the tweet id — required; rows without it dropped), text, author, posted_at, url, metrics, subject=target.subject, company_id/user_id from target, source='x-twitter', fetched_at set.
    - Rows missing post_id are dropped (natural-key anchor; never fabricate an id).
    - Empty/absent results → [].
    - No field is invented — absent fields map to null.
  </behavior>
  <action>
Implement pure helpers appOnlyLaneReady(doctorResult: unknown): boolean and mapTweetsResult(results: unknown, target: IngestTarget): XPostInsert[] in `lib/ingest/x-twitter.ts`, free of `server-only` so vitest loads them. Write `lib/ingest/x-twitter.test.ts` covering the doctor gate (ready / missing / absent lane) and the mapper (normal tweets, missing post_id, empty) with inline fixtures modeled on the doctor + sync envelope shapes.
  </action>
  <verify>
    <automated>npx vitest run lib/ingest/x-twitter.test.ts</automated>
  </verify>
  <done>All tests pass; appOnlyLaneReady gates correctly on the app_only_api lane; tweets without post_id are dropped; empty → []; no fabricated fields.</done>
</task>

<task type="auto">
  <name>Task 2: ingestXTwitter SourceModule — preflight, sync, upsert (opt-in, read-only)</name>
  <files>lib/ingest/x-twitter.ts</files>
  <read_first>lib/ingest/cli.ts, lib/agents/sentiment.ts, lib/types.ts (XPostInsert)</read_first>
  <action>
Implement `ingestXTwitter(admin, targets): Promise<SourceSummary>`. Preflight in order: (1) requireEnv("X_BEARER_TOKEN") — absent → return status "skipped", detail "X_BEARER_TOKEN not set (opt-in)"; (2) hasBinary("x-twitter-pp-cli") — absent → skipped; (3) runAgentCli doctor and appOnlyLaneReady(result) — not ready → skipped. Pass X_BEARER_TOKEN through the env to runAgentCli; never log its value.

For each target, run `sync --resources tweets --since <window>` (choose a reasonable recent window, e.g. 14d; document it), scoping the query by the target's subject/handle. Use ONLY read subcommands (sync/recent-search) — never any post/reply/quote subcommand. Map via mapTweetsResult and `admin.from("x_posts").upsert(rows, { onConflict: "company_id,post_id" })` with user_id set explicitly from target.userId (service-role bypasses RLS — scope writes to the owner). Accumulate upserted/skipped; status "partial" on per-target errors else "success". Never throw out of the loop. Add `import "server-only"` only if it does not break vitest loading of the pure helpers.

Enforce the read-only invariant with a single allowlist constant of permitted subcommands (`["doctor","sync","recent-search"]`) used to build every runAgentCli call — no post/reply/quote string appears anywhere in the module.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint lib/ingest/x-twitter.ts && npx vitest run lib/ingest/x-twitter.test.ts</automated>
  </verify>
  <done>tsc + eslint + vitest clean; absent X_BEARER_TOKEN / binary / app-only lane → clean skip; posts upsert on (company_id, post_id) with user_id from the target; only allowlisted read subcommands are used (no post/reply/quote anywhere in the file); the bearer token is env-only and never logged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| x-twitter stdout → cache | untrusted external tweet JSON, parsed via parseEnvelope, mapped defensively |
| target subject/handle → CLI arg | DB-derived, passed to execFile (array, no shell) |
| X_BEARER_TOKEN → CLI env | paid secret; env-only, never logged |
| service-role write → x_posts | RLS bypassed; user_id must be set from target.userId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Information Disclosure | X_BEARER_TOKEN | mitigate | requireEnv only; passed via env to execFile; never logged; missing → clean skip |
| T-04-16 | Elevation of Privilege | write subcommands | mitigate | allowlist of read subcommands (doctor/sync/recent-search) builds every call; no post/reply/quote string in the module; read-only bearer lane |
| T-04-17 | Denial of Service / cost | paid per-read | mitigate | opt-in on X_BEARER_TOKEN presence + doctor preflight; bounded `--since` window; per-target try/catch |
| T-04-18 | Elevation of Privilege | service-role upsert | mitigate | user_id set from target.userId; parameterized upsert on (company_id, post_id) |
| T-04-SC | Tampering | package installs | mitigate | no new packages; x-twitter-pp-cli is a pre-installed local binary; slopcheck N/A |
</threat_model>

<verification>
- `npx vitest run lib/ingest/x-twitter.test.ts` green (doctor gate + tweets mapper).
- `npx tsc --noEmit` + `npx eslint lib/ingest/x-twitter.ts` clean.
- Code review confirms the read-only subcommand allowlist and absence of any post/reply/quote invocation.
- Manual (optional, requires X_BEARER_TOKEN): `npm run ingest` populates x_posts; without the token the source is skipped.
</verification>

<success_criteria>
x-twitter company + competitor posts are synced read-only via the app-only bearer
lane and cached source-tagged in x_posts, opt-in on X_BEARER_TOKEN and idempotent
(ING-04, ING-06).
</success_criteria>

<output>
Create `.planning/phases/04-external-grounding-ingestion/04-05-SUMMARY.md` when done.
</output>
