---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components/company/confidence-chip.tsx
  - components/company/confidence-chip.test.tsx
  - lib/analysis/staleness.ts
  - lib/analysis/staleness.test.ts
autonomous: true
requirements: [FND-05, FND-06]

must_haves:
  truths:
    - "A shared chip renders a Fact/Estimate badge plus a Low/Med/High confidence indicator, built on the existing Badge, following the design system"
    - "A compact 'Run deep-dive' empty-state primitive exists for enriched areas before first generation"
    - "A pure isStale helper reports whether underlying data changed after generated_at, so a 'may be stale' hint can render"
  artifacts:
    - path: "components/company/confidence-chip.tsx"
      provides: "ConfidenceChip + DeepDiveEmpty primitives"
      exports: ["ConfidenceChip", "DeepDiveEmpty"]
      min_lines: 30
    - path: "lib/analysis/staleness.ts"
      provides: "pure isStale(generatedAt, latestDataChange)"
      exports: ["isStale"]
  key_links:
    - from: "components/company/confidence-chip.tsx"
      to: "components/ui/badge.tsx"
      via: "Badge variant composition"
      pattern: "from \"@/components/ui/badge\""
    - from: "components/company/confidence-chip.tsx"
      to: "lib/agents/deep-dive-types.ts"
      via: "LabelledField basis/confidence literals"
      pattern: "LabelledField|basis|confidence"
---

<objective>
Build the shared confidence-labelling primitive (Fact/Estimate + Low/Med/High chip)
on top of the existing `Badge`, plus the compact "Run deep-dive" empty-state and a
pure staleness helper. These are consumed by the trigger (Plan 04) and by every
enriched area in Phases 2–4. This is a leaf UI/util slice — independent of the
agent — so it runs in parallel with Plan 02.

Purpose: FND-05 (shared fact/estimate + confidence chip on labelled fields) and
FND-06 (empty-state CTA + "may be stale" hint helper).
Output: `confidence-chip.tsx` (ConfidenceChip + DeepDiveEmpty), `staleness.ts` (isStale), tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@docs/superpowers/specs/2026-07-02-company-deep-dive-analysis-design.md
@.planning/codebase/CONVENTIONS.md
@components/ui/badge.tsx
@components/company/provenance.tsx
@lib/agents/deep-dive-types.ts

<interfaces>
<!-- Contracts and design tokens the chip implements against. -->

From components/ui/badge.tsx:
- `Badge` accepts `variant`: default | secondary | success | destructive | outline | muted,
  and a `className`. It renders a `div` — pass extra classes to compose.

From lib/agents/deep-dive-types.ts (Plan 01):
- `LabelledField` — { text, basis: "fact" | "estimate", confidence: "low" | "med" | "high", source? }.
  The chip's props should accept `basis` and `confidence` using these exact literals.

Design system (CONVENTIONS.md "Premium Minimal Flat"):
- Ink-on-white, hairline (1px `border`), `.tabular-nums` for numbers,
  `.label-eyebrow` for tiny labels. Muted/success at reduced opacity (e.g. bg-success/15).
  Do NOT introduce saturated brand color as a default; keep chips quiet.
- provenance.tsx already renders Badge variants for source/corroboration — match that visual weight.

Server/client boundary (CONVENTIONS.md): a plain presentational chip with no hooks or
lucide icon props can be a Server Component. If DeepDiveEmpty renders a lucide icon or a
client action, it (or the icon-bearing part) must be `"use client"`. Keep ConfidenceChip
free of forwardRef icon props so it can be used from server components.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure staleness helper (tested)</name>
  <read_first>
    - docs/superpowers/specs/2026-07-02-company-deep-dive-analysis-design.md (§3 staleness: generated_at vs. underlying valuations/competitors changes)
    - .planning/phases/01-foundation/01-CONTEXT.md (FND-06 "may be stale" hint)
    - lib/competitors/rank.test.ts (co-located pure-test style)
  </read_first>
  <files>lib/analysis/staleness.ts, lib/analysis/staleness.test.ts</files>
  <behavior>
    - isStale(generatedAt, latestChange) === true when latestChange is strictly after generatedAt.
    - === false when latestChange is before/equal to generatedAt.
    - === false when generatedAt is null/undefined (no analysis yet → empty state, not stale).
    - === false when latestChange is null (no underlying change data).
    - Accepts ISO date strings and/or Date; parses safely, returns false on unparseable input.
  </behavior>
  <action>
    Create `lib/analysis/staleness.ts` exporting a pure `isStale(generatedAt: string | Date | null | undefined, latestDataChange: string | Date | null | undefined): boolean` that returns true only when both timestamps parse and `latestDataChange > generatedAt`. Add a short doc comment explaining it drives the "may be stale" hint (FND-06) and is observational only. Create `lib/analysis/staleness.test.ts` (Vitest) covering the behaviors above. Keep it dependency-free and pure.
  </action>
  <acceptance_criteria>
    - `npm run test -- staleness` passes with ≥ 4 assertions covering the behaviors.
    - `grep -c "export function isStale\|export const isStale" lib/analysis/staleness.ts` == 1.
    - Null-generatedAt case returns false (asserted).
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- staleness</automated>
  </verify>
  <done>isStale is a tested pure helper that reports whether underlying data changed after generated_at, defaulting to not-stale on missing inputs.</done>
</task>

<task type="auto">
  <name>Task 2: ConfidenceChip + DeepDiveEmpty primitives</name>
  <read_first>
    - components/ui/badge.tsx (variant API to compose on)
    - components/company/provenance.tsx (existing Badge usage / visual weight to match)
    - .planning/codebase/CONVENTIONS.md (design system tokens; server/client boundary rule for lucide icons)
    - lib/agents/deep-dive-types.ts (basis/confidence literals)
    - .agents/skills/ui-ux-pro-max/SKILL.md (design intelligence — apply premium-minimal-flat)
  </read_first>
  <files>components/company/confidence-chip.tsx, components/company/confidence-chip.test.tsx</files>
  <action>
    Create `components/company/confidence-chip.tsx` exporting `ConfidenceChip` — a small
    presentational component (per FND-05) taking `basis: "fact" | "estimate"` and
    `confidence: "low" | "med" | "high"` (Claude's discretion whether one component with
    variants or a Fact/Estimate badge + a confidence dot; the CONTEXT lists this as
    discretion). Render a Fact/Estimate `Badge` (use `secondary`/`muted` variants so it
    stays quiet — Fact reads slightly more affirmed than Estimate, e.g. `success`-tinted at
    low opacity for fact, `muted` for estimate) plus a Low/Med/High confidence indicator
    (three-step dot/segment or a muted mini-label using `.label-eyebrow`). Follow the design
    system: hairline border, reduced-opacity tints, no saturated brand default, `.tabular-nums`
    if any number appears. Also export `DeepDiveEmpty` — a compact empty-state block (per
    FND-06) with a short "Run deep-dive to generate this analysis" message and a slot/prop
    for an action (accept an optional `action?: React.ReactNode` so Plan 04 can drop the
    button in), styled quietly to avoid clutter. Keep `ConfidenceChip` free of hooks and
    lucide-icon props so it is usable from Server Components; if `DeepDiveEmpty` uses a lucide
    icon, add `"use client"` to the file per the boundary rule (Claude's discretion — prefer
    keeping the chip server-safe). Write `components/company/confidence-chip.test.tsx`
    asserting the rendered output includes the expected Fact/Estimate label and a distinct
    element per confidence level (basic render/snapshot-style assertions; if the repo's Vitest
    env is node-only and cannot render React, instead unit-test a pure label/variant-mapping
    helper you extract from the component and export). Do NOT use `dangerouslySetInnerHTML`.
  </action>
  <acceptance_criteria>
    - `grep -c "dangerouslySetInnerHTML" components/company/confidence-chip.tsx` == 0.
    - `grep -c "export function ConfidenceChip\|export const ConfidenceChip" components/company/confidence-chip.tsx` == 1 and DeepDiveEmpty is exported.
    - Chip imports Badge from `@/components/ui/badge` (grep hit).
    - `npx tsc --noEmit`, `npm run lint`, and `npm run test` are green (the chip test or the extracted mapping test passes).
    - Uses `basis`/`confidence` literals matching LabelledField (`"med"`, not `"medium"`).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && [ "$(grep -c dangerouslySetInnerHTML components/company/confidence-chip.tsx)" = "0" ] && grep -q "@/components/ui/badge" components/company/confidence-chip.tsx && npm run test && echo OK</automated>
  </verify>
  <done>ConfidenceChip renders a design-system Fact/Estimate + Low/Med/High chip built on Badge, DeepDiveEmpty provides the compact empty-state CTA slot, no XSS sink, tsc/lint/test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored analysis text → React render | Web-derived, LLM-authored strings are displayed via these primitives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering (XSS) | ConfidenceChip / DeepDiveEmpty render | mitigate | Render text as React children only; no `dangerouslySetInnerHTML` (grep gate == 0) so React auto-escapes LLM-derived strings. |
| T-01-09 | Information Disclosure | client bundle | accept | These are presentational primitives with no secrets or data access; no server-only concern. |
| T-01-SC | Tampering | npm installs | accept | No new packages — reuses installed Badge/lucide/Vitest; nothing to audit. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean, `npm run lint` clean, `npm run test` green.
- No dangerouslySetInnerHTML; chip built on existing Badge; design-system compliant.
</verification>

<success_criteria>
- Shared Fact/Estimate + Low/Med/High chip exists and follows the design system.
- Compact "Run deep-dive" empty-state primitive exists with an action slot.
- Pure isStale helper backs the "may be stale" hint.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done.
</output>
