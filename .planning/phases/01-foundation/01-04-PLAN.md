---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - app/(app)/companies/actions.ts
  - components/company/deep-dive-button.tsx
  - app/(app)/companies/[id]/page.tsx
autonomous: true
requirements: [FND-04, FND-06]

must_haves:
  truths:
    - "A 'Run deep-dive' header button on the company detail page triggers generation on demand, separate from Sync, with staged progress mirroring SyncButton"
    - "A server action invokes runDeepDive under the RLS user client and revalidates the company page"
    - "Re-running overwrites the stored row with a fresh generated_at"
    - "Before first generation, an enriched area shows the compact 'Run deep-dive' empty state; a 'may be stale' hint shows when underlying data changed after generated_at"
  artifacts:
    - path: "app/(app)/companies/actions.ts"
      provides: "runDeepDiveAction server action"
      contains: "runDeepDive"
    - path: "components/company/deep-dive-button.tsx"
      provides: "client Run deep-dive button with staged progress"
      exports: ["DeepDiveButton"]
      min_lines: 30
  key_links:
    - from: "components/company/deep-dive-button.tsx"
      to: "app/(app)/companies/actions.ts"
      via: "runDeepDiveAction call in useTransition"
      pattern: "runDeepDiveAction"
    - from: "app/(app)/companies/actions.ts"
      to: "lib/agents/deep-dive.ts"
      via: "runDeepDive(supabase, company) invocation + revalidatePath"
      pattern: "runDeepDive"
    - from: "app/(app)/companies/[id]/page.tsx"
      to: "components/company/deep-dive-button.tsx"
      via: "header action render"
      pattern: "DeepDiveButton"
---

<objective>
Wire the generation engine to the UI: a `runDeepDiveAction` server action that runs
`runDeepDive` under the RLS user client and revalidates the company page, a "Run
deep-dive" header button (client, staged progress mirroring SyncButton, separate
from Sync, re-run overwrites), and the empty-state + "may be stale" hint surfaced on
the page using `getCompanyAnalysis` + `isStale`. This is the final vertical-slice
task — after it, a user can click a button and generate a stored analysis.

Purpose: FND-04 (on-demand trigger, staged progress, timestamped overwrite) + the
UI half of FND-06 (empty state before first run, staleness hint).
Output: server action, `deep-dive-button.tsx`, header + empty-state wiring on the page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@docs/superpowers/specs/2026-07-02-company-deep-dive-analysis-design.md
@.planning/codebase/CONVENTIONS.md
@components/company/sync-button.tsx
@app/(app)/companies/actions.ts

<interfaces>
<!-- Contracts from Plans 01–03 and existing patterns to wire against. -->

From Plan 02 — lib/agents/deep-dive.ts:
- `runDeepDive(supabase, company)` — takes the Supabase client + CompanyWithRelations,
  makes one Grok call, upserts one company_analysis row, returns a small result object.

From Plan 01 — lib/queries.ts:
- `getCompanyAnalysis(companyId): Promise<CompanyAnalysisRow | null>` — the reader for
  the stored row (has `generated_at`).

From Plan 03:
- `components/company/confidence-chip.tsx` exports `DeepDiveEmpty` (empty-state block with
  an `action?` slot) and `ConfidenceChip`.
- `lib/analysis/staleness.ts` exports `isStale(generatedAt, latestDataChange)`.

From components/company/sync-button.tsx (the staged-progress pattern to MIRROR, not reuse):
- `"use client"`; `useTransition` + local `done`/`error` state; calls the server action;
  on success `router.refresh()`; button shows spinner label while pending, done label briefly.
  Mirror this for staged progress copy (e.g. "Gathering context…" → "Analysing…") — exact
  copy is Claude's discretion.

From app/(app)/companies/actions.ts (server-action conventions):
- `"use server"` file; `requireUser()` returns `{ supabase, user }`; guard
  `if (!user) return { error: "Not authenticated." }`; return `ActionResult` ({ error?, id? });
  best-effort external calls wrapped in try/catch; `revalidatePath` affected routes.

From app/(app)/companies/[id]/page.tsx (header actions block at ~line 213):
- The header actions live in `<div className="flex flex-wrap items-center gap-2">` next to
  `<SyncButton companyId={company.id} />`. Add `<DeepDiveButton companyId={company.id} />`
  adjacent to it (separate control). The page is a Server Component that already has `company`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: runDeepDiveAction server action</name>
  <read_first>
    - app/(app)/companies/actions.ts (requireUser, ActionResult, revalidatePath, try/catch degrade patterns)
    - lib/agents/deep-dive.ts (runDeepDive signature — Plan 02)
    - lib/queries.ts (getCompany to load CompanyWithRelations if the action needs it)
    - .planning/codebase/CONVENTIONS.md (server action conventions, never throw to client)
  </read_first>
  <files>app/(app)/companies/actions.ts</files>
  <action>
    Add an exported `runDeepDiveAction(companyId: string): Promise<ActionResult>` to
    `app/(app)/companies/actions.ts`. Use `requireUser()`; return `{ error: "Not authenticated." }`
    if no user. Load the company (with relations) via the existing query/read so it can be
    passed to `runDeepDive` (or fetch the row the agent needs) — reuse `getCompany`-style read
    with the RLS user client so ownership is enforced. Call `runDeepDive(supabase, company)`
    inside try/catch, returning `{ error: message }` on failure rather than throwing (per FND-04
    re-run overwrites — the agent's upsert handles timestamping). On success `revalidatePath`
    the company detail route (`/companies/[id]` — match the existing revalidate style used by
    syncCompany) and return `{}`/`{ id: companyId }`. Do NOT use the admin client here — the
    button-triggered action must run under RLS as the current user.
  </action>
  <acceptance_criteria>
    - `grep -c "export async function runDeepDiveAction" app/(app)/companies/actions.ts` == 1.
    - Action calls `runDeepDive` and `revalidatePath` (both grep hits present).
    - Action guards `if (!user)` (grep hit) and does not import/use `createAdminClient`.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "runDeepDiveAction" "app/(app)/companies/actions.ts" && grep -q "runDeepDive(" "app/(app)/companies/actions.ts" && grep -q "revalidatePath" "app/(app)/companies/actions.ts" && ! grep -q "createAdminClient" "app/(app)/companies/actions.ts" && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>runDeepDiveAction runs runDeepDive under the RLS user client, degrades gracefully, and revalidates the company page; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: DeepDiveButton + header/empty-state/stale wiring</name>
  <read_first>
    - components/company/sync-button.tsx (useTransition + staged status pattern to mirror)
    - components/company/confidence-chip.tsx (DeepDiveEmpty with action slot — Plan 03)
    - lib/analysis/staleness.ts (isStale — Plan 03)
    - lib/queries.ts (getCompanyAnalysis — Plan 01)
    - app/(app)/companies/[id]/page.tsx (header actions block ~line 213; server component with `company`)
  </read_first>
  <files>components/company/deep-dive-button.tsx, app/(app)/companies/[id]/page.tsx</files>
  <action>
    Create `components/company/deep-dive-button.tsx` (`"use client"`) exporting `DeepDiveButton`
    that mirrors `SyncButton`: `useTransition`, local `error`/`done` state, calls
    `runDeepDiveAction(companyId)` (Task 1); show STAGED progress copy while pending (e.g.
    "Gathering context…" then "Analysing…" — a small staged label, exact copy Claude's
    discretion), a done state, and `router.refresh()` on success. Use the existing `Button`
    (size `sm`, `variant="outline"`) so it reads as a distinct control SEPARATE from Sync;
    add `role="progressbar"`/`aria-live` on the status text for accessibility (mirror the
    intent described in CONTEXT). Then in `app/(app)/companies/[id]/page.tsx`: import and render
    `<DeepDiveButton companyId={company.id} />` in the header actions `<div className="flex
    flex-wrap items-center gap-2">` beside `<SyncButton />`. In the page (Server Component),
    read the stored analysis via `getCompanyAnalysis(company.id)`; when it is null, render the
    `DeepDiveEmpty` primitive (Plan 03) in one enriched area passing `<DeepDiveButton />` as its
    action slot (proves the empty-state CTA — FND-06); when it exists, compute staleness with
    `isStale(analysis.generated_at, latestDataChange)` where `latestDataChange` is the most
    recent `updated_at`/`created_at` among the company's valuations/competitors already loaded on
    the page, and render a subtle "may be stale" hint when true (plus the `generated_at` date).
    Keep changes behavior-preserving for all existing tabs — only add; do not alter existing
    rendering. Do NOT pass a lucide icon prop across the server→client boundary except through
    the already-client button.
  </action>
  <acceptance_criteria>
    - `grep -c "export function DeepDiveButton\|export const DeepDiveButton" components/company/deep-dive-button.tsx` == 1 and file starts with `"use client"`.
    - Button calls `runDeepDiveAction` and uses `useTransition` (grep hits).
    - Page imports `DeepDiveButton`, `getCompanyAnalysis`, `isStale`, and `DeepDiveEmpty` (grep each in page.tsx).
    - Page still renders `<SyncButton` (existing control preserved — grep hit) — deep-dive is additive/separate.
    - `npx tsc --noEmit`, `npm run lint`, `npm run test`, and `npm run build` all pass.
  </acceptance_criteria>
  <verify>
    <automated>head -1 components/company/deep-dive-button.tsx | grep -q "use client" && grep -q "runDeepDiveAction" components/company/deep-dive-button.tsx && grep -q "useTransition" components/company/deep-dive-button.tsx && grep -q "DeepDiveButton" "app/(app)/companies/[id]/page.tsx" && grep -q "getCompanyAnalysis" "app/(app)/companies/[id]/page.tsx" && grep -q "isStale" "app/(app)/companies/[id]/page.tsx" && grep -q "SyncButton" "app/(app)/companies/[id]/page.tsx" && npx tsc --noEmit && npm run lint && npm run build && echo OK</automated>
  </verify>
  <done>A "Run deep-dive" header button (staged progress, separate from Sync) triggers generation; the page shows the empty-state CTA before first run and a "may be stale" hint when underlying data is newer than generated_at; build/tsc/lint/test green and existing tabs unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server action | Only the authenticated owner may trigger generation for their company |
| stored analysis → page render | generated_at / stale hint / empty-state derived from owner-scoped reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Elevation of Privilege | runDeepDiveAction | mitigate | Action uses `requireUser()` + the RLS user client (not admin); a user can only load and generate for a company they own — RLS blocks cross-owner reads/writes. |
| T-01-11 | Tampering (XSS) | staleness hint / empty state render | mitigate | All rendered strings pass through React children (auto-escaped); no `dangerouslySetInnerHTML`; date rendered via existing formatting helpers. |
| T-01-12 | Denial of Service | repeated deep-dive triggers | accept | Single-user portfolio tool; button disabled while pending (useTransition) bounds concurrency; no additional rate-limit needed for v1. |
| T-01-SC | Tampering | npm installs | accept | No new packages — reuses installed Button/lucide/next; nothing to audit. |
</threat_model>

<verification>
- `npm run build`, `npx tsc --noEmit`, `npm run lint`, `npm run test` all green.
- Button is separate from Sync, runs under RLS, staged progress present.
- Empty-state CTA shows pre-generation; "may be stale" hint shows when data is newer than generated_at.
- Existing tabs behavior-preserved.
</verification>

<success_criteria>
- On-demand "Run deep-dive" trigger works, separate from Sync, re-run overwrites/timestamps.
- Empty state + staleness hint render per FND-06.
- No regressions; quality gates green.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done.
</output>
