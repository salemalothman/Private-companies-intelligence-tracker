# Private Companies Intelligence Tracker — Deep-Dive Analysis

## What This Is

A Next.js 15 / React 19 / Supabase web app for tracking a private-company
investment portfolio: per-company valuations, funding rounds, competitor
benchmarking, news, and documents, backed by an LLM/web enrichment pipeline
(xAI/Grok, Exa, SEC EDGAR). This body of work adds a **per-company "deep-dive
analysis" layer** — an institutional-grade business/technology/competitive/
valuation analysis generated from data the app already holds — distributed across
the existing company tabs, plus a new comps-based **Valuation Targets (2026–2030)**
tab.

## Core Value

Every generated insight is **grounded and honestly labelled** — real facts vs.
confidence-tagged estimates vs. transparent comps math — never fabricated
financials or invented probabilities.

## Requirements

### Validated

<!-- Inferred from the existing codebase map (.planning/codebase/). Shipped and relied upon. -->

- ✓ Company detail page with 8 tabs (Overview, Provenance, Data room, Investment,
  Valuation, Funding Rounds, Competitors, News) — existing
- ✓ Supabase typed client + RLS (user) and service-role admin (cron); hand-
  maintained `lib/types.ts` Database schema — existing
- ✓ Enrichment/ingestion pipeline: `lib/agents/` (global-sync, financials,
  refresh, sentiment, exa-events), `lib/enrichment/` (sanitize-sources,
  timeline-validation, disambiguation), `lib/ingestion/` — existing
- ✓ Canonical reconciliation across sources (`lib/canonical.ts`) with fact/
  estimate provenance and confidence — existing
- ✓ Connectors: xAI/Grok, Exa web search, SEC EDGAR, market-cache (`lib/connectors/`) — existing
- ✓ Competitor discovery + ranking with valuation, revenue, V/R multiple, and
  SEC-verified badges (`lib/competitors/`, `buildCompetitorRanking`) — existing
- ✓ Business-model segment mix estimate (`lib/business-model.ts`,
  `BusinessModelAnalysis`) — existing
- ✓ Data-integrity guardrails: monotonic timeline validation, trusted-source
  classification, source sanitization — existing
- ✓ Design system: premium minimal flat (ink-on-white, hairline, tabular-nums);
  shared `CollapsibleSection`/`SectionEmpty`; `cn` = twMerge(clsx) — existing
- ✓ Vitest suite (140 tests) + tsc + eslint gates — existing

### Active

<!-- The deep-dive feature. Hypotheses until shipped. -->

- [ ] Structured deep-dive analysis generated by a single Grok agent, stored as
  JSONB per company (`company_analysis` table), refreshed on demand
- [ ] Shared Fact/Estimate + Low/Med/High confidence chip reused wherever
  generated content appears
- [ ] Overview tab enriched with the thesis layer (exec summary, core tech + moat
  rating, product portfolio, vertical/customer, unit economics, TAM/SAM/SOM,
  strategic moat, historical analogue, Outlook & Exit narrative, IC conclusion)
- [ ] Competitors tab enriched with threat tiers (direct / indirect / emerging)
  and a Capability Matrix (IP Depth · GTM Velocity · Capital Efficiency ·
  Workflow Retention, 1–10 vs top 3 threats)
- [ ] Valuation tab enriched with factual financial detail (margins/burn/runway/
  ACV where retrievable)
- [ ] New **Valuation Targets (2026–2030)** tab: comps model
  (revenue × peer V/R multiple), Bear/Base/Bull scenarios, agent-proposed +
  user-adjustable growth and multiple percentile, live recompute

### Out of Scope

- **§15 fabricated 10-year P&L forecast model** — violates the no-fabricated-
  financials guardrail
- **Asserted probability tables** (IPO-by-year %, acquisition %, scenario %
  splits) — violates the no-predictive-risk-metrics guardrail
- **Exact $ valuation targets not derived from comps** — only transparent
  peer-multiple math is allowed
- **New "Thesis" catch-all tab** — decision was to distribute into existing tabs
  (plus the one dedicated Valuation Targets tab)

## Context

- Full approved design spec:
  `docs/superpowers/specs/2026-07-02-company-deep-dive-analysis-design.md`
- Source material is a 19-section institutional deep-dive prompt, **adapted** (not
  implemented verbatim) to respect project guardrails ("hybrid" mode).
- Codebase map: `.planning/codebase/` (STACK, ARCHITECTURE, STRUCTURE,
  CONVENTIONS, TESTING, INTEGRATIONS, CONCERNS).
- Grounding inputs for generation already exist in-app: canonical record,
  competitor ranking + multiples, funding/valuation history, news, documents.
- Recent fix of note: lucide `forwardRef` icons cannot cross the RSC server→client
  boundary — client components must own icon rendering.

## Constraints

- **Data integrity**: No fabricated financial data; no predictive risk metrics.
  Forward-looking content must carry `basis: fact|estimate` and
  `confidence: low|med|high`. Quantitative valuation only via transparent
  peer-multiple comps.
- **Tech stack**: Next.js 15 App Router, React 19, TypeScript, Tailwind, Supabase
  (typed client + RLS), Vitest. Match existing patterns.
- **Generation**: One structured Grok pass per company → stored JSONB; comps
  inputs (multiples, base revenue) computed in code, not by the LLM.
- **UX**: On-demand generation (separate from Sync); reuse `CollapsibleSection`
  and the design system; empty/stale states.
- **Security**: `.env.local` gitignored; never commit secrets; service-role/cron
  secrets stay in Vercel env.
- **Quality gates**: tsc clean, eslint clean, Vitest green after each phase;
  behavior-preserving for existing tabs.

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Hybrid speculation (qualitative forward-looking + confidence; no fabricated quantitative models) | Honors no-fabricated-data guardrail while keeping analyst value | — Pending |
| Distribute narrative into existing tabs (no Thesis tab) + one new Valuation Targets tab | User preference; keeps raw-data tabs as the substrate | — Pending |
| One structured Grok agent → `company_analysis` JSONB, on demand | Matches canonical/global-sync pattern; cheap reads, cacheable | — Pending |
| Valuation Targets = comps model; agent proposes growth, user overrides growth % + multiple percentile, live recompute | Comps from real inputs is defensible; user-adjustable assumptions make it bulletproof | — Pending |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd:complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-07-02 after initialization*
