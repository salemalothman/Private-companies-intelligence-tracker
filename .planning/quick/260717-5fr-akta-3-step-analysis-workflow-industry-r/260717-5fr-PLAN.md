---
phase: quick-260717-5fr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/connectors/akta.ts
  - lib/connectors/akta.test.ts
  - lib/competitors/discover.ts
  - lib/competitors/discover.test.ts
  - lib/agents/deep-dive.ts
autonomous: true
requirements: [AKTA-STEP2, AKTA-STEP3, AKTA-VALMETRIC]
user_setup: []

must_haves:
  truths:
    - "Competitor sync merges Grok + akta industry-news competitors; akta wins duplicate names"
    - "The target company's akta financial estimate reaches canonical inputs (all connectors' fetchValuationMetric are queried, not just the first)"
    - "Deep-dive generation grounds the Grok pass with akta deep-search articles AND persists them as news rows"
    - "When AKTA_API_KEY is absent, every new path no-ops without throwing"
    - "At most 2 akta news calls per competitor-discovery run and 2 per deep-dive generation"
  artifacts:
    - path: "lib/connectors/akta.ts"
      provides: "AktaConnector.fetchCompetitors (industry-resolved) + exported aktaDeepSearch + pure extractors"
      contains: "fetchCompetitors"
    - path: "lib/connectors/akta.test.ts"
      provides: "HTTP-free unit tests for the industry-mention extractor (multiple field shapes) + deep-search normalizer"
    - path: "lib/competitors/discover.ts"
      provides: "multi-source additive merge (akta-wins) + all-connector valuation-metric gather"
      contains: "fetchValuationMetric"
    - path: "lib/agents/deep-dive.ts"
      provides: "akta deep-search grounding block + news persistence via applyMappedIngest"
  key_links:
    - from: "lib/competitors/discover.ts"
      to: "AktaConnector.fetchCompetitors"
      via: "getConnectors().filter(fetchCompetitors)"
      pattern: "fetchCompetitors"
    - from: "lib/agents/deep-dive.ts"
      to: "applyMappedIngest"
      via: "news persistence path"
      pattern: "applyMappedIngest"
---

<objective>
Integrate akta.pro's "Company Analysis & Enrichment Workflow" into the pipeline as
its remaining two steps (step 1, firmographic enrichment, already exists via
`AktaConnector.fetchCompanyProfile` — do NOT duplicate it):

- Step 2 (competitor discovery) joins the SYNC pipeline: industry-resolved,
  entity-mention-ranked competitors from akta industry news, additively merged
  with Grok's competitors — akta wins duplicate names. Also fix a known gap: the
  target's own valuation metric is currently taken from only the first
  competitor-capable connector, so akta's trusted "akta.pro financial estimate"
  never reaches canonical inputs.
- Step 3 (deep search) runs ONLY during DEEP-DIVE ANALYSIS GENERATION: topic-based
  akta news search feeds the Grok pass as grounded evidence (facts + URLs) AND is
  persisted as news rows (akta's native sentiment, publisher-domain source with
  "akta.pro" fallback, deduped against existing news).

Purpose: grounds two more surfaces in the same-domain, entity-resolved akta data
without fabricating figures (data-integrity constraint) and closes the valuation-
metric gap.
Output: extended `AktaConnector`, reworked `discoverCompetitors`, deep-search
grounding + news persistence in `runDeepDive`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md

<interfaces>
<!-- Executor: use these directly, no codebase exploration needed. -->

lib/connectors/types.ts — DataConnector optional methods:
  fetchCompetitors?(query: string, hint?: string): Promise<ConnectorCompetitor[]>
  fetchValuationMetric?(query: string): Promise<Omit<ConnectorCompetitor, "name"> | null>
  ConnectorCompetitor { name; valuation?; valuationDate?; revenue?; revenueBasis?; basis?; source }
  ConnectorNewsItem { title; source; url?; date?; summary?; sentiment?: "positive"|"neutral"|"negative" }

lib/connectors/akta.ts (existing — EXTEND, do not rewrite):
  - SOURCE = "akta.pro"; AKTA_BASE = "https://api.akta.pro/api"
  - async function aktaGet(path, params): Promise<unknown|null>  // handles x-api-key, envelope {data,credits_consumed}, returns null on any failure — never throws
  - function section(data, name): unknown  // unwrap bare-vs-keyed section
  - helpers: clean(), isoDate(), mapSentiment(), publisherDomain()  // reuse
  - AktaConnector.resolveCompany(query): Promise<AktaSearchHit|null>  // AktaSearchHit { uuid?; name?; website?; product_category?; company_status? }
  - existing exported pure mappers: mapAktaProfile, mapAktaNews, mapAktaFinancial
  - existing methods: fetchCompanyProfile, fetchFundingRounds(→[]), fetchNews, fetchValuationMetric

lib/competitors/discover.ts (existing — REWORK discoverCompetitors):
  - getConnectors() from @/lib/connectors/registry returns DataConnector[] gated on env keys
  - lookupMarketValuations(supabase, names): Promise<Map<nameKey, MarketValuationRow>>; nameKey from @/lib/market-cache/parse
  - applyCache(base, cachedRow), cacheSource(row), secFallback(sec, name, hasValuation)
  - DiscoveredCompetitor extends ConnectorCompetitor { secVerified }
  - CompetitorDiscovery { competitors: DiscoveredCompetitor[]; self: Omit<ConnectorCompetitor,"name">|null }

lib/agents/deep-dive.ts (existing — WIRE deep search into runDeepDive):
  - runDeepDive(supabase, company: CompanyWithRelations): Promise<{error?}>
  - company has: id, user_id, name, sector, description, news[]
  - grounding assembled by summarizeGrounding(); prompt built from buildPrompt(grounding)
  - `grounding` string is composed near line 803-806 before the Grok call

lib/ingestion/apply.ts:
  - applyMappedIngest(supabase, companyId, { fundingRounds: [], valuations: [], news: ConnectorNewsItem[] }): Promise<ApplyResult>
    // dedupes news by lowercased title against existing rows; classifies + inserts. USE THIS to persist deep-search news (respects the "no direct table mutation outside applyMappedIngest" anti-pattern).

lib/canonical.ts:
  - provider("akta.pro") === "akta"; canonical tie-break already prefers akta observations. No change needed here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend AktaConnector with industry-resolved fetchCompetitors + aktaDeepSearch</name>
  <files>lib/connectors/akta.ts, lib/connectors/akta.test.ts</files>
  <behavior>
    Pure industry-mention extractor (export it, HTTP-free):
    - Given industry-news articles, extract entity-resolved company mentions,
      exclude the target company (normalized case-insensitive name match), rank
      by mention frequency (desc), map each to
      ConnectorCompetitor { name, basis: "akta.pro industry-news mention", source: "akta.pro" }.
    - LENIENT zod schema (z.object({...}).passthrough(), optional fields): accept
      mentions under any of `companies`, `company_mentions`, `mentions`, each an
      array of { name | company_name, uuid? }. Test a fixture for EACH shape.
    - Empty/malformed input → []. Never throws.
    Pure deep-search normalizer (export it): raw akta articles →
      { title, url, date, summary, sentiment, source } where source is the
      publisher domain (reuse publisherDomain()) falling back to "akta.pro",
      sentiment via mapSentiment(), date via isoDate(). Untitled → dropped.
    Industry-code resolver: given industry-search hits [{code, industry_name,
      similarity}], keep similarity >= 0.45, cap at 3 codes, joined comma-separated.
  </behavior>
  <action>
    Extend the EXISTING lib/connectors/akta.ts (keep `import "server-only"`, never-throw
    contract, exported-pure-mapper pattern). Add:
    1. Exported pure functions: `resolveIndustryCodes(hits, {floor=0.45, cap=3})`,
       `extractIndustryMentions(articles, targetName)`, `normalizeDeepSearchArticles(articles)`.
       Write the raw akta mention shapes with a lenient passthrough zod schema that
       accepts the three plausible field names/object shapes documented above.
    2. `AktaConnector.fetchCompetitors(query, hint?)`: resolveCompany (free) →
       resolve industries via GET /v1/industry/search?query=<sector-or-hint-or-name>
       (free) → resolveIndustryCodes → ONE GET /v1/news?industry=<codes>&limit=25&group_articles=true
       → extractIndustryMentions(articles, resolvedName). Absent key / no company /
       no codes / no articles → []. HARD cost guard: at most 2 akta news calls per
       call (here just 1; leave headroom, do not loop unbounded).
    3. Exported `aktaDeepSearch(companyRef, topics: string[])`: run 1-2
       GET /v1/news?query=<topic>&company=<uuid> calls (topics passed in from
       deep-dive), merge + normalizeDeepSearchArticles, dedupe by title. Cap at 2
       news calls. Absent key / unresolved company → []. Never throws.
    Reuse aktaGet, section, clean, isoDate, mapSentiment, publisherDomain. Do NOT
    add env vars — everything gated on existing AKTA_API_KEY via aktaGet's own guard.
    Co-locate tests in lib/connectors/akta.test.ts (pattern: existing mapper tests).
  </action>
  <verify>
    <automated>npx vitest run lib/connectors/akta.test.ts</automated>
  </verify>
  <done>fetchCompetitors + aktaDeepSearch + 3 exported pure fns exist; extractor tests pass for `companies`/`company_mentions`/`mentions` shapes and empty input; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rework discoverCompetitors — multi-source merge (akta-wins) + all-connector valuation gather</name>
  <files>lib/competitors/discover.ts, lib/competitors/discover.test.ts</files>
  <action>
    Rework `discoverCompetitors` (keep cache-first behavior, SEC fallback, self-row
    logic, DiscoveredCompetitor/CompetitorDiscovery shapes):
    1. Sources = ALL connectors implementing fetchCompetitors (not just the first).
       Query each with Promise.allSettled semantics; per-source try/catch → [] so
       one source's failure never aborts. Keep the existing empty-set single retry
       ONLY for the primary/Grok source (a real company almost always has peers);
       do not retry akta (cost guard).
    2. MERGE lists additively; on duplicate names (normalized: trim + lowercase,
       reuse the existing target-exclusion normalization) the akta row WINS — an
       akta-sourced competitor (source contains "akta") replaces a same-name row
       from another source. Still drop self-references to the target and intra-list
       dupes as today.
    3. Valuation metric: call fetchValuationMetric on ALL implementers (best-effort
       Promise.all, per-connector try/catch → null); feed EVERY non-null observation
       into the self record path so akta's "akta.pro financial estimate" reaches
       canonical inputs (canonical tie-break in lib/canonical.ts already prefers
       akta). Preserve cache-hit-wins-over-live for the target's self metric.
    Fixes the known gap where only the first fetchCompetitors source's valuation
    metric was used. Update/extend co-located discover.test.ts to cover: akta-wins
    on a duplicate name, and multiple valuation-metric observations gathered.
  </action>
  <verify>
    <automated>npx vitest run lib/competitors/discover.test.ts</automated>
  </verify>
  <done>Two competitor-capable connectors merge additively; a same-name akta row overrides the other source; all implementers' fetchValuationMetric queried; degrades to [] when a source throws; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire akta deep-search into runDeepDive — grounding block + news persistence</name>
  <files>lib/agents/deep-dive.ts</files>
  <action>
    In `runDeepDive`, BEFORE the Grok call (where `grounding` is assembled around
    lines 803-806): derive 1-2 deep-search topics IN CODE from the company profile
    (e.g. `"${company.name} product reviews market position"` plus a sector topic
    `"${company.sector} ${company.name}"` when sector is present) and call the new
    exported `aktaDeepSearch` (Task 1) with the company name/ref. Cost guard: at
    most 2 akta news calls per generation.
    1. GROUNDING: fold the returned articles into the prompt as a distinct
       source-tagged evidence block (title + url + date + summary, marked as akta.pro
       news facts with URLs) appended to `grounding` — same UNTRUSTED-DATA framing
       the existing grounding uses (do not let article text act as instructions;
       reuse the groundingText() sanitizer for free-text fields). Empty result →
       omit the block (no empty header, no fabricated lines).
    2. PERSIST: write the same articles as news rows via
       `applyMappedIngest(supabase, company.id, { fundingRounds: [], valuations: [], news: articles })`
       — this dedupes by title against existing news and applies akta's native
       sentiment + publisher-domain source. Do NOT raw-insert into the news table
       (respects the "no direct table mutation outside applyMappedIngest" anti-pattern).
       Best-effort: wrap in try/catch so a persistence failure never fails the
       deep-dive generation.
    Gated implicitly on AKTA_API_KEY (aktaDeepSearch no-ops → [] when absent). No
    schema/lib/types.ts change — the news table already exists.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run lib/agents/deep-dive.test.ts</automated>
  </verify>
  <done>runDeepDive calls aktaDeepSearch (≤2 news calls), appends a grounding block when articles exist, persists them via applyMappedIngest; absent-key path no-ops; tsc + existing deep-dive tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| akta API → app | Untrusted external JSON (news articles, entity mentions) crosses here |
| akta article text → Grok prompt | Third-party free text folded into an LLM prompt (injection surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5fr-01 | Tampering | akta mention/article JSON | mitigate | Lenient passthrough zod parse in pure extractors; malformed → []; never throw (matches aktaGet contract) |
| T-5fr-02 | Elevation (prompt injection) | deep-search article text → Grok prompt | mitigate | Fold articles inside existing UNTRUSTED-DATA `<<<GROUNDING>>>` markers; run free-text through groundingText() (strips control chars / U+2028-9, collapses whitespace, truncates) so an article cannot forge its own `basis: fact` bullet |
| T-5fr-03 | Denial (cost/credits) | akta news calls | mitigate | Hard cap: ≤2 news calls per discovery run and ≤2 per deep-dive; no unbounded loops; no akta retry |
| T-5fr-SC | Tampering | npm/pip installs | accept | No new dependencies added — uses existing zod, no package installs |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npm run lint` clean
- `npm run test` green (new akta + discover tests, existing deep-dive tests)
- With AKTA_API_KEY unset, discover + deep-dive still complete (akta paths return [] / no-op)
</verification>

<success_criteria>
- Competitor sync merges Grok + akta industry-news competitors; akta wins on duplicate names
- Target's akta financial estimate reaches canonical inputs (all connectors' fetchValuationMetric queried)
- Deep-dive grounds the Grok pass with akta deep-search articles AND persists them as deduped news rows
- ≤2 akta news calls per competitor-discovery run and ≤2 per deep-dive generation
- All new paths no-op gracefully without AKTA_API_KEY; no schema / lib/types.ts changes
- tsc, eslint, vitest all green
</success_criteria>

<output>
Create `.planning/quick/260717-5fr-akta-3-step-analysis-workflow-industry-r/260717-5fr-SUMMARY.md` when done
</output>
