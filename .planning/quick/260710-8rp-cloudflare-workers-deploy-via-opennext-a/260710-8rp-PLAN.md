---
phase: quick-260710-8rp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - wrangler.jsonc
  - open-next.config.ts
  - next.config.mjs
  - .dev.vars.example
  - .gitignore
autonomous: true
requirements: [CF-DEPLOY-01]
user_setup: []

must_haves:
  truths:
    - "`npx opennextjs-cloudflare build` completes cleanly and emits `.open-next/worker.js` + `.open-next/assets`"
    - "`npm run dev` (next dev) still starts — the next.config.mjs change is additive and does not break the Node/Vercel path"
    - "tsc, eslint, and vitest remain green"
    - "vercel.json is unmodified; the Vercel build path still works"
    - ".dev.vars and .open-next/ are gitignored (no secrets or build output committed)"
  artifacts:
    - path: "wrangler.jsonc"
      provides: "Cloudflare Worker config pointing at OpenNext output"
      contains: "nodejs_compat"
    - path: "open-next.config.ts"
      provides: "OpenNext Cloudflare adapter config (default dummy incremental cache)"
      contains: "defineCloudflareConfig"
    - path: ".dev.vars.example"
      provides: "Local workerd runtime secret template mirroring .env.local.example"
    - path: "package.json"
      provides: "preview:cf / deploy:cf / cf-typegen scripts + @opennextjs/cloudflare devDependency"
      contains: "preview:cf"
  key_links:
    - from: "wrangler.jsonc"
      to: ".open-next/worker.js"
      via: "main field"
      pattern: "\\.open-next/worker\\.js"
    - from: "next.config.mjs"
      to: "@opennextjs/cloudflare"
      via: "initOpenNextCloudflareForDev()"
      pattern: "initOpenNextCloudflareForDev"
---

<objective>
Add a Cloudflare Workers deployment path to this Next.js 15.1.3 App Router app via
`@opennextjs/cloudflare`, without disturbing the existing Vercel deployment.

Purpose: Give the project a second, self-hostable deploy target (Cloudflare Workers)
while keeping Vercel as the primary path — both build from the same Next.js source.
Output: `wrangler.jsonc`, `open-next.config.ts`, an additive `next.config.mjs` edit,
new `package.json` scripts + devDependency, `.dev.vars.example`, and gitignore entries.
Deploy is OUT of scope (wrangler is not authenticated); the acceptance gate is a clean
`opennextjs-cloudflare build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Current config the executor must preserve / additively extend
@next.config.mjs
@package.json
@.env.local.example
@.gitignore

<interfaces>
<!-- Confirmed API shape from current @opennextjs/cloudflare docs. Use directly. -->

open-next.config.ts:
```typescript
import { defineCloudflareConfig } from "@opennextjs/cloudflare";
// No R2/ISR infra for a plain SSR + Supabase app — omit incrementalCache to use
// the built-in dummy cache. Do NOT import r2-incremental-cache.
export default defineCloudflareConfig({});
```

next.config.mjs (additive — ESM):
```javascript
import { initOpenNextCloudflareForDev } from "@opennextjs/cloudflare";
// ...existing nextConfig unchanged...
export default nextConfig;
initOpenNextCloudflareForDev(); // enables Cloudflare bindings during `next dev`
```

wrangler.jsonc key fields: `main: ".open-next/worker.js"`, `name`,
`compatibility_date` (recent, >= 2024-09-23), `compatibility_flags:
["nodejs_compat", "global_fetch_strictly_public"]`, `assets: { directory:
".open-next/assets", binding: "ASSETS" }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install adapter and create Cloudflare/OpenNext config files</name>
  <files>package.json, wrangler.jsonc, open-next.config.ts, next.config.mjs</files>
  <action>
    Install the adapter as a devDependency: run `npm install --save-dev @opennextjs/cloudflare`
    (official OpenNext package; wrangler ^4.86.0 is already a devDependency — do not touch it).

    Create `wrangler.jsonc` at repo root with, per the confirmed docs interface:
    `"$schema": "node_modules/wrangler/config-schema.json"`, `"name": "private-companies-tracker"`,
    `"main": ".open-next/worker.js"`, a recent `"compatibility_date"` (use "2025-05-05" or newer),
    `"compatibility_flags": ["nodejs_compat", "global_fetch_strictly_public"]`, and
    `"assets": { "directory": ".open-next/assets", "binding": "ASSETS" }`.
    Do NOT add the WORKER_SELF_REFERENCE service or IMAGES binding — they are only needed for
    ISR/cache and Cloudflare image optimization, which this plain SSR app does not use. If the
    build in Task 3 later demands one, the executor may add it then per the local package docs.

    Create `open-next.config.ts` at repo root using `defineCloudflareConfig({})` exactly as shown
    in the interfaces block — empty config so it falls back to the built-in dummy incremental cache.
    Do NOT wire R2 or any external cache.

    Edit `next.config.mjs` ADDITIVELY: add `import { initOpenNextCloudflareForDev } from "@opennextjs/cloudflare";`
    at the top, and call `initOpenNextCloudflareForDev();` on the line AFTER `export default nextConfig;`.
    Leave `serverExternalPackages`, `images.remotePatterns`, and
    `experimental.serverActions.bodySizeLimit` exactly as they are. Do NOT touch vercel.json.
  </action>
  <verify>
    <automated>test -f wrangler.jsonc && test -f open-next.config.ts && grep -q "@opennextjs/cloudflare" package.json && grep -q "initOpenNextCloudflareForDev" next.config.mjs && grep -q "serverExternalPackages" next.config.mjs && echo OK</automated>
  </verify>
  <done>@opennextjs/cloudflare in devDependencies; wrangler.jsonc + open-next.config.ts exist with correct fields; next.config.mjs additively imports and calls initOpenNextCloudflareForDev with all prior config intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add CF scripts, .dev.vars.example, and gitignore entries</name>
  <files>package.json, .dev.vars.example, .gitignore</files>
  <action>
    Add three scripts to `package.json` "scripts" (keep existing scripts untouched):
    `"preview:cf": "opennextjs-cloudflare build && opennextjs-cloudflare preview"`,
    `"deploy:cf": "opennextjs-cloudflare build && opennextjs-cloudflare deploy"`,
    `"cf-typegen": "wrangler types --env-interface CloudflareEnv cloudflare-env.d.ts"`.

    Create `.dev.vars.example` mirroring `.env.local.example` — the same variable keys and
    placeholder values (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY,
    SUPABASE_SERVICE_ROLE_KEY, and the commented optional keys). Add a top comment noting this is
    the local workerd/`opennextjs-cloudflare preview` runtime secret template — copy to `.dev.vars`.

    Edit `.gitignore` additively: under a new `# cloudflare / opennext` comment add `.dev.vars`,
    `.dev.vars.*` (but keep `.dev.vars.example` tracked — since `.dev.vars.*` would match it, instead
    add `.dev.vars` alone plus `!.dev.vars.example` is unnecessary; simplest: add the two lines
    `.dev.vars` and `.open-next/`, and `cloudflare-env.d.ts`). Ensure `.dev.vars.example` is NOT ignored.
  </action>
  <verify>
    <automated>grep -q "preview:cf" package.json && grep -q "deploy:cf" package.json && test -f .dev.vars.example && grep -q "NEXT_PUBLIC_SUPABASE_URL" .dev.vars.example && grep -q ".open-next" .gitignore && grep -q ".dev.vars" .gitignore && git check-ignore .dev.vars.example; test $? -ne 0 && echo OK</automated>
  </verify>
  <done>package.json has preview:cf/deploy:cf/cf-typegen; .dev.vars.example mirrors .env.local.example and is NOT gitignored; .gitignore ignores .dev.vars, .open-next/, and cloudflare-env.d.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Verify OpenNext build is clean and existing gates stay green</name>
  <files>(no source changes — verification only)</files>
  <action>
    Run the OpenNext build as the acceptance gate: `npx opennextjs-cloudflare build`. It must complete
    without error and produce `.open-next/worker.js` and `.open-next/assets`. Do NOT run
    `opennextjs-cloudflare preview` (interactive/long-running) and do NOT deploy (wrangler unauthenticated).

    If the build fails demanding a binding or config the task-1 minimal wrangler.jsonc omitted (e.g.
    WORKER_SELF_REFERENCE service, IMAGES binding, or an incremental-cache override), consult the locally
    installed package README/types under `node_modules/@opennextjs/cloudflare` and add ONLY the minimum
    the error requires — keep the change scoped and re-run the build.

    Then confirm the existing quality gates are unaffected: `npx tsc --noEmit` clean, `npm run lint` clean,
    `npm run test` (vitest) green. Confirm the Vercel path is untouched: `git status` shows vercel.json
    unmodified, and next.config.mjs changes are additive only (initOpenNextCloudflareForDev + import).
  </action>
  <verify>
    <automated>npx opennextjs-cloudflare build && test -f .open-next/worker.js && npx tsc --noEmit && npm run lint && npm run test && git status --porcelain vercel.json | grep -q . && echo "VERCEL_MODIFIED_FAIL" || echo OK</automated>
  </verify>
  <done>`opennextjs-cloudflare build` produces .open-next/worker.js; tsc/lint/vitest all green; vercel.json unmodified; next.config.mjs change confirmed additive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependencies | New `@opennextjs/cloudflare` package installed into the build toolchain |
| local secrets → git | `.dev.vars` holds real runtime secrets for workerd preview; must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8rp-SC | Tampering | `@opennextjs/cloudflare` npm install | mitigate | Official OpenNext-org package (opennext.js.org); pairs with already-installed wrangler; installed as devDependency only, not shipped to app runtime. Legitimacy established — no separate human-verify gate required. |
| T-8rp-01 | Information Disclosure | `.dev.vars` local secrets | mitigate | `.dev.vars` added to .gitignore in Task 2; only `.dev.vars.example` (placeholders) is tracked. Task 2 verify asserts `.dev.vars.example` is NOT ignored while `.dev.vars` IS. |
| T-8rp-02 | Denial of Service | Vercel deploy path regression | accept/mitigate | next.config.mjs edited additively only; vercel.json untouched (asserted in Task 3 verify). `next dev` and Vercel build continue using the same nextConfig object. |
</threat_model>

<verification>
- `npx opennextjs-cloudflare build` exits 0 and emits `.open-next/worker.js` + `.open-next/assets`.
- `npx tsc --noEmit`, `npm run lint`, `npm run test` all pass.
- `git status` confirms vercel.json unmodified and next.config.mjs changes additive.
- `git check-ignore .dev.vars` matches; `git check-ignore .dev.vars.example` does NOT match.
</verification>

<success_criteria>
- Cloudflare Workers deploy path exists (wrangler.jsonc + open-next.config.ts + scripts) and builds cleanly.
- Existing Vercel path (vercel.json, next.config.mjs behavior, `npm run dev`) is preserved.
- No secrets or build output committed; all quality gates green.
</success_criteria>

<output>
Create `.planning/quick/260710-8rp-cloudflare-workers-deploy-via-opennext-a/260710-8rp-SUMMARY.md` when done.
</output>
