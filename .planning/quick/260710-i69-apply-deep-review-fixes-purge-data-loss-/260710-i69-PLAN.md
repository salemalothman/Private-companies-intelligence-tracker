---
quick_id: 260710-i69
title: Apply deep-review fixes (Critical→Low)
autonomous: true
---

# Deep-review remediation — session diff cff860c..HEAD

Behavior-preserving. Add regression tests. Gates after each commit: `npx tsc --noEmit`,
`npx next lint`, `npm run test` all green. Commit atomically per group below.

## Commit 1 — CRITICAL: stop the purge deleting legitimate rows
`lib/enrichment/disambiguation.ts`, `lib/enrichment/disambiguation.test.ts`
- Add an opts flag to `wrongEntitySignal` (e.g. `{ isPrivate?, scope?: "ingest" | "purge" }`)
  OR give `purgeWrongEntitySignals` a narrow-mode call so the DELETE path uses only
  COLLISION_RULES + STOCK_SIGNAL (its pre-session scope) — NOT EXCHANGE_SYMBOL/FINANCE_DOMAIN.
- Drop `finance.yahoo.` and `investing.com` from FINANCE_DOMAIN (keep tradingview,
  stockanalysis, marketscreener, wallmine).
- Restrict EXCHANGE_SYMBOL so a mere third-party ticker mention doesn't block (title-only
  or name-adjacent).
- Tests: "Acme acquired by Salesforce (NYSE:CRM)" NOT purged; finance.yahoo.com funding
  item NOT purged; TSE:4395 wrong-entity STILL dropped at ingest.

## Commit 2 — Correctness mediums + dead code in the guard
`lib/enrichment/disambiguation.ts` (+test), `lib/agents/exa-events.ts`
- EXCHANGE_SYMBOL: drop `/i` on the prefix alternation; require uppercase symbol after the
  colon so "big six:", "she:", "sha:" prose no longer matches. Test prose passes through.
- `isGenericMultiCompanyReport`: normalize stored name (strip Inc/Ai/Io/LLC/Corp/Ltd,
  first-token compare) before REPORT_KEYWORDS so "Moove hits $2B valuation" for "Moove Io"
  is NOT dropped. Test it.
- Remove unreachable rule 2 + EXCHANGE_COUNTRY map (rule 1 blocks first for private cos);
  fix the misleading test.
- Remove unused `founded_year` (CompanyEventContext, exa-events select + call site).
- Simplify EventScreenResult (drop always-null `value` or keep passthrough — remove
  invalid states); fix the exa-events "replaces a fabricated figure" comment.

## Commit 3 — HIGH: cron failure isolation + drift guard + worker typecheck
`worker/cron-dispatch.ts` (new) + `worker/cron-dispatch.test.ts` (new),
`worker/index.ts`, `worker/tsconfig.json` (new), `worker/open-next-worker.d.ts` (new),
`package.json`, `.github/workflows/deploy.yml`, `.github/workflows/preview.yml`
- Extract CRON_ROUTES + pure `dispatchScheduled(cron, fetchFn, secret)` into cron-dispatch.ts
  (no .open-next import). Per-path try/catch (log + continue), keep 06:00 ordering.
- Tests: ordering, unmapped no-op, bearer header, and Object.keys(CRON_ROUTES) === wrangler
  triggers.crons (parse wrangler.jsonc).
- worker/index.ts becomes a thin shell importing the helper.
- Add @cloudflare/workers-types devDep + worker/tsconfig.json + a d.ts stub for
  ../.open-next/worker.js (removes @ts-expect-error); add `tsc -p worker` to both workflows.

## Commit 4 — HIGH: render recovery-flow errors
`app/(auth)/login/page.tsx`, `components/auth/auth-form.tsx`
- Read searchParams.error; map invalid-link/recovery-failed/recovery-expired to messages;
  render in AuthForm's error slot with role="alert" + link to /forgot-password.

## Commit 5 — Medium security: allowlist the reset host
`lib/request-origin.ts` (+test), `app/(auth)/actions.ts`
- Validate x-forwarded-host against known hosts (*.workers.dev, *.vercel.app, localhost)
  before use; fall back to siteUrl() otherwise.

## Commit 6 — Dead exports
`lib/metrics.ts`
- Delete `unrealizedGainSeries` + `companyCountSeries` (zero refs).

## Commit 7 — Auth a11y + DRY
`components/auth/submit-button.tsx` (new), auth-shell if clean, `auth-form.tsx`,
`forgot-password-form.tsx`, `update-password-form.tsx`, `reset-password/page.tsx`,
`lib/auth-constants.ts` (new), `app/(auth)/actions.ts`
- Extract SubmitButton; extract AuthShell (revert + note if server/client boundary breaks).
- autoComplete new-password/current-password/email; role="alert"+aria on errors;
  forgot-password success role="status" + focus; MIN_PASSWORD_LENGTH=6 constant;
  grow forgot link touch target; success confirmation for dialog path; "At least 6
  characters" hint with aria-describedby.

## Commit 8 — Dashboard a11y + honesty
`components/motion/animated-number.tsx`, `components/dashboard/portfolio-charts.tsx`,
`components/app/mobile-nav.tsx`
- AnimatedNumber: aria-hidden animating span + sr-only finalText sibling.
- portfolio-charts: "Other" slice for sectors beyond top 5; human month labels
  ("Jul 2026", UTC) for tick + tooltip; role="img"+aria-label on the three charts.
- mobile-nav: add ChangePasswordDialog by signOut.

## Commit 9 — CI nits
`.github/workflows/*`, `.github/actions/build-and-test/action.yml` (if clean)
- Derive worker name from wrangler.jsonc; fail preview URL step if empty.
- Composite action for the shared build chain (revert + note if it breaks).
