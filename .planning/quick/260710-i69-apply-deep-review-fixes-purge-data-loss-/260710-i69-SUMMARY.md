---
quick_id: 260710-i69
title: Apply deep-review fixes (Critical→Low)
completed: 2026-07-10
commits: 9
gates: tsc clean · tsc -p worker clean · eslint clean · 299 tests green
---

# Deep-review remediation — SUMMARY

All nine plan commit-groups applied behavior-preservingly, each committed
atomically with its own regression tests. Every gate (`npx tsc --noEmit`,
`npx tsc -p worker`, `npm run lint`, `npm run test`) is green after every commit
and on the final tree (299 tests, up from 291 at start).

## Findings → commits

| # | Severity | Finding | Commit | Key files |
|---|----------|---------|--------|-----------|
| 1 | CRITICAL | Wrong-entity purge could DELETE legitimate rows (broad EXCHANGE_SYMBOL/FINANCE_DOMAIN gates ran on the destructive path) | `27130a2` | `lib/enrichment/disambiguation.ts` (+test) |
| 2 | Medium×3 + dead code | EXCHANGE_SYMBOL matched lowercase prose; generic-report guard dropped first-token headlines; unreachable country rule + unused `founded_year` + invalid `EventScreenResult.value` states | `a5497e4` | `lib/enrichment/disambiguation.ts` (+test), `lib/agents/exa-events.ts` |
| 3 | HIGH | Cron dispatch had no per-route failure isolation; worker entrypoint was untypechecked (`@ts-expect-error`) | `c852a9f` | `worker/cron-dispatch.ts` + test (new), `worker/index.ts`, `worker/tsconfig.json` + `open-next-worker.d.ts` (new), `package.json`, both workflows |
| 4 | HIGH | Recovery-flow errors (`invalid-link`/`recovery-failed`/`recovery-expired`) were silently swallowed on the login page | `89da66c` | `app/(auth)/login/page.tsx`, `components/auth/auth-form.tsx` |
| 5 | Medium (security) | `x-forwarded-host` used unvalidated for reset links (Host-header open redirect) | `20fb6e0` | `lib/request-origin.ts` (+test), `app/(auth)/actions.ts` |
| 6 | Low | Dead exports `unrealizedGainSeries` + `companyCountSeries` | `0ff4221` | `lib/metrics.ts` |
| 7 | Low (a11y/DRY) | Duplicated SubmitButton/shell; missing autoComplete, alert/status roles, password hints, min-length constant | `a60ff01` | `components/auth/{submit-button,auth-shell}.tsx` (new), `auth-form.tsx`, `forgot-password-form.tsx`, `update-password-form.tsx`, `change-password-dialog.tsx`, `reset-password/page.tsx`, `lib/auth-constants.ts` (new), `app/(auth)/actions.ts` |
| 8 | Low (a11y/honesty) | AnimatedNumber announced intermediate untrue values; allocation chart silently dropped sectors beyond 5; machine month labels; charts had no img role; mobile had no change-password | `3f3c37e` | `components/motion/animated-number.tsx`, `components/dashboard/portfolio-charts.tsx`, `components/app/mobile-nav.tsx` |
| 9 | Low (CI) | Duplicated build chain; hardcoded worker name; preview URL step could post empty | `bfbc33e` | `.github/actions/build-and-test/action.yml` (new), `.github/workflows/{deploy,preview}.yml` |

## Commit 1 (CRITICAL) — regression coverage

The mandatory regressions all pass:
- `"Acme acquired by Salesforce (NYSE:CRM)"` is NOT purged (peer ticker, not name-adjacent) — nor blocked at ingest.
- A `finance.yahoo.com` funding item is NOT purged.
- `TSE:4395` wrong-entity is STILL dropped at ingest (collision rule + STOCK_SIGNAL) and at purge.

Mechanism: `wrongEntitySignal` gained a `scope: "ingest" | "purge"` option; the
purge (DELETE) path runs only the narrow pre-session gate (COLLISION_RULES +
STOCK_SIGNAL). Exchange-ticker matching was additionally restricted to
name-adjacent tickers, and `finance.yahoo.*` / `investing.com` were dropped from
the finance-domain list (broad publishers that also carry real funding news).

## Revertible items (per plan) — both kept

- **AuthShell extraction (Commit 7):** kept. The shell holds no hooks/client
  APIs, so the `reset-password` server page reuses it without a boundary break
  (tsc + build-order verified via `tsc --noEmit`).
- **CI composite action (Commit 9):** kept. `.github/actions/build-and-test`
  is consumed by both workflows after `actions/checkout`; all three YAML files
  parse, and the worker-name node extraction + preview grep pattern were
  validated locally against `wrangler.jsonc` and a sample wrangler output.

## Notable deviations / environment handling

- **[Rule 3 — blocking] node_modules symlink restored.** `npm install
  --save-dev @cloudflare/workers-types` (required by Commit 3) replaced the
  worktree's `node_modules` symlink with a real 1 GB directory, which broke the
  ESLint `@next/next` plugin dedup the nested worktree relies on (two ancestor
  `.eslintrc.json` files + a shared symlinked `node_modules`). Lint went red on
  *all* files, including untouched ones. Fixed by copying `@cloudflare/workers-types`
  into the shared `node_modules` and restoring the `../../../node_modules`
  symlink (per project memory). Lint returned to green. `package.json` /
  `package-lock.json` still carry the new devDep (committed in Commit 3).
- **Ambient module stub (Commit 3):** an exact-relative `declare module
  "../.open-next/worker.js"` is not honoured by TS for a non-existent file; a
  wildcard specifier (`"*/.open-next/worker.js"`) is, so the stub uses that.
- **Change-password dialog success (Commit 7):** the dialog now auto-closes
  after a 1.5s delay so the `role="status"` "Password updated." confirmation is
  seen/announced first (previously closed instantly with no confirmation).

## Verification note

The login recovery-error rendering (Commit 4) and the auth/dashboard a11y
changes (Commits 7–8) are visually observable but were verified via
tsc/lint/test only; a browser pass over the login `?error=…` states, the
forgot/reset forms, and the dashboard charts is recommended but not blocking.

## Self-Check: PASSED

- New files present: `worker/cron-dispatch.ts`, `worker/cron-dispatch.test.ts`,
  `worker/tsconfig.json`, `worker/open-next-worker.d.ts`,
  `components/auth/submit-button.tsx`, `components/auth/auth-shell.tsx`,
  `lib/auth-constants.ts`, `.github/actions/build-and-test/action.yml`.
- All 9 commits present in `git log` (27130a2 … bfbc33e); working tree clean.
- Final gates: root tsc 0, worker tsc 0, eslint 0, 299/299 tests pass.
