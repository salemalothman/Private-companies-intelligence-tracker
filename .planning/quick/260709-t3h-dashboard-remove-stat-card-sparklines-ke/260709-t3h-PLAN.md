---
phase: quick
plan: 260709-t3h
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/summary-cards.tsx
  - app/(app)/dashboard/page.tsx
  - components/dashboard/portfolio-charts.tsx
autonomous: false
requirements: [QUICK-DASH-01, QUICK-DASH-02]

must_haves:
  truths:
    - "The four dashboard stat cards show only icon + number + label (no sparkline)"
    - "Full stat values render without truncation (e.g. '$154.88' not '$154.88' clipped)"
    - "Full stat labels render without ellipsis (e.g. 'Invested Capital', 'Unrealized Gain')"
    - "The Allocation-by-sector donut has a clean, empty center (no TOTAL overlay)"
    - "The allocation total dollar amount is shown as a readable stat line outside the donut"
  artifacts:
    - path: "components/dashboard/summary-cards.tsx"
      provides: "Stat cards without sparkline"
      contains: "SummaryCards"
    - path: "components/dashboard/portfolio-charts.tsx"
      provides: "Allocation donut with total relocated to header"
      contains: "Allocation by sector"
  key_links:
    - from: "app/(app)/dashboard/page.tsx"
      to: "components/dashboard/summary-cards.tsx"
      via: "SummaryCards prop wiring"
      pattern: "SummaryCards"
---

<objective>
Two presentation-only dashboard tweaks requested from a live-app annotation:

1. Remove the sparkline mini-graphs from the four stat cards (Portfolio Value,
   Invested Capital, Unrealized Gain, Companies). The sparkline currently steals
   horizontal space, truncating the number and ellipsizing the label. After
   removal, icon + number + full label must fit cleanly.
2. In the "Allocation by sector" donut, remove the hard-to-read center
   "TOTAL $X" overlay and surface the total as a readable stat line in the card
   header, leaving the donut center clean.

Purpose: Restore legibility of the KPI strip and allocation total.
Output: Updated summary-cards.tsx, portfolio-charts.tsx, and dashboard page wiring.

CONSTRAINT: Presentation-only. Do NOT change any data derivation in lib/metrics.ts.
Follow the design system (CLAUDE.md): keep `.tabular-nums` on every financial
figure, hairline borders, ink-on-white premium-minimal-flat aesthetic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
From lib/metrics.ts (already imported in page.tsx — types only, no derivation changes):
- portfolioSummary(companies) → PortfolioSummary
- portfolioValueSeries / investedCapitalSeries → PortfolioValuePoint[]  (still needed by PortfolioCharts)
- unrealizedGainSeries / companyCountSeries → PortfolioValuePoint[]      (ONLY consumed by the sparklines — becomes unused after Task 1)
- sectorAllocation(companies) → SectorSlice[]  ({ sector: string; value: number })

Current SummaryCards signature (components/dashboard/summary-cards.tsx):
  SummaryCards({ summary: PortfolioSummary; series: SummarySeries })
  interface SummarySeries { value; invested; gain; count: PortfolioValuePoint[] }
  Card container: `flex items-center justify-between gap-3 ... p-5`, left block
  `min-w-0`, label div uses `truncate`, right side renders <Sparkline>.

Current allocation total (portfolio-charts.tsx ~line 268-274):
  Absolute-positioned overlay centered in the donut, label-eyebrow "Total" +
  `formatCurrency(allocationTotal)`. `allocationTotal` is computed at line ~105.
  The "Portfolio value" card header (lines 118-135) is the pattern to mirror for
  an inline stat line: label-eyebrow block + `text-xl font-semibold tabular-nums`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove sparklines from the stat cards</name>
  <files>components/dashboard/summary-cards.tsx, app/(app)/dashboard/page.tsx</files>
  <action>
In components/dashboard/summary-cards.tsx: delete the `Sparkline` component
(function and its `<Sparkline .../>` usage in the card map), delete the
`SummarySeries` interface, delete the `series` field from the `cards[]` objects,
and drop the `series` prop from `SummaryCards`. Remove now-unused imports:
`Area, AreaChart, ResponsiveContainer` from "recharts", the `PortfolioValuePoint`
type import, and the `ACCENTS[].stroke` values are now unused — keep only `.chip`
(icon chip color) since the icon chip stays. Simplify the card container: since
there is no right-hand sparkline, drop `flex items-center justify-between gap-3`
in favor of a plain block card (`rounded-2xl border border-border bg-card p-5`),
remove the `min-w-0` wrapper constraint, and remove `truncate` from the label div
so full labels render. Keep icon chip, the `AnimatedNumber`/value figure with
`tabular-nums` and existing `numberClass` (hero gradient / success / destructive),
and the `sub` suffix. Do not alter values, formats, or accent colors.

In app/(app)/dashboard/page.tsx: change `<SummaryCards summary={summary}
series={{...}} />` to `<SummaryCards summary={summary} />`. Remove the now-unused
metric imports `investedCapitalSeries`, `unrealizedGainSeries`,
`companyCountSeries` from the "@/lib/metrics" import — BUT keep
`portfolioValueSeries` and `investedCapitalSeries` ONLY if still referenced by the
`<PortfolioCharts>` call below (they are: `valueSeries={portfolioValueSeries(...)}`
and `investedSeries={investedCapitalSeries(...)}`). So the net removal is
`unrealizedGainSeries` and `companyCountSeries` only. Update the "Every sparkline
series is REAL recorded data" comment above SummaryCards to reflect removal.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>tsc + eslint clean. summary-cards.tsx has no recharts import and no Sparkline. Each card renders icon + number + full label with no truncate. page.tsx passes only `summary` to SummaryCards; no unused metric imports remain.</done>
</task>

<task type="auto">
  <name>Task 2: Move allocation donut total out of the center to the header</name>
  <files>components/dashboard/portfolio-charts.tsx</files>
  <action>
In components/dashboard/portfolio-charts.tsx, in the "Allocation by sector"
ChartCard: delete the absolute-positioned center overlay div (the
`pointer-events-none absolute inset-0 ...` block with label-eyebrow "Total" +
`formatCurrency(allocationTotal)`), leaving the donut center empty. Since the
overlay wrapper `<div className="relative h-full w-full">` no longer needs
`relative` positioning for the overlay, keep the ResponsiveContainer but the
wrapper can be simplified to `h-full w-full`. Relocate the total into the card
`header` prop: mirror the "Portfolio value" header stat pattern — add a right-
aligned stat line reading label-eyebrow "Total" over
`text-xl font-semibold tabular-nums` with `formatCurrency(allocationTotal)`.
Restructure the allocation header so the title + legend chips stay on the left
and the Total stat sits at the top-right of the header row (use a flex
justify-between wrapper so the legend chips wrap beneath the title without
colliding with the Total). Keep `allocationTotal` computed as-is (line ~105).
Do not change the RadialBarChart, rings, colors, tooltip, or any derivation.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>tsc + eslint clean. Donut center is empty (no overlay). Allocation card header shows title + legend on the left and a "Total $X" stat line (tabular-nums) on the right. allocationTotal derivation unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Removed the four stat-card sparklines (full number + label now fit) and moved the allocation donut total from the center overlay to a header stat line.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open /dashboard.
    2. Confirm the four KPI cards (Portfolio Value, Invested Capital, Unrealized
       Gain, Companies) show icon + full number + full label with NO sparkline
       and NO truncation/ellipsis (e.g. "Invested Capital" fully visible, the
       Portfolio Value figure not clipped).
    3. Confirm the "Allocation by sector" donut center is empty and the total
       dollar amount appears as a legible stat line in the card header.
    4. Confirm the rest of the dashboard (hero value curve, top performers,
       changes list) is unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `npm run lint` clean
- `npm run test` green (no test touches these presentation components; confirm no regression)
- Visual human-verify checkpoint passed
</verification>

<success_criteria>
- Stat cards: icon + number + full label, no sparkline, no truncation
- Allocation donut: clean empty center, total shown as a readable header stat line
- No data derivations changed (lib/metrics.ts untouched)
- tsc / eslint / vitest all clean
</success_criteria>

<output>
Create `.planning/quick/260709-t3h-dashboard-remove-stat-card-sparklines-ke/260709-t3h-SUMMARY.md` when done
</output>
